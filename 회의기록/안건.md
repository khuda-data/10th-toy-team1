# 안건함 — 다음 회의에서 다룰 것들

> 작업하다 **막힌 문제**, **팀이 정해야 할 사안**이 생기면 즉시 아래에 한 줄 추가하세요 (누구나, 순서 상관없음).
> AI도 자동으로 추가하게 되어 있습니다 (AGENTS.md §2-7). 회의에서 다룬 항목은 지우고 회의록으로 옮깁니다.

| 올린 날짜 | 올린 사람 | 안건 (막힌 것 / 정할 것) | 관련 문서 |
|---|---|---|---|
| 2026-08-18 | wjdwlsah | **오버샘플링 완화용 `sample_weight`를 `tune_model`의 검증 fold 채점(scoring)에도 반영할지** — 지금은 `model__sample_weight`로 학습(fit)에만 반영되고, `GridSearchCV`가 하이퍼파라미터를 고르는 기준인 scoring에는 반영되지 않음. 이대로 두면 "학습은 사람 단위로 공평하게, 모델 선택 기준은 여전히 반복 인원 쪽에 쏠린 채점"이 되는 비일관성이 있음. scoring까지 반영하려면 커스텀 스코어러 구현이 추가로 필요함 | `code/model/tune.py`, `plan/details/06-인터페이스.md` |
| 2026-08-17 | jungjinmo | **`major_group` Group A 백필이 `NotApplicable` 재분류(`e0f5de7`)와 충돌해 983명분 과거 전공값이 유실됨 — 이미 main에 반영돼 있음** — `_backfill_group_a`의 `needs_fill = filled[column].isna()`가 baseline이 이미 `"NotApplicable"`(NaN 아님)이면 백필 탐색을 아예 안 함. 실제 raw 데이터 재실행으로 확인: 2022년 baseline 345명·2023년 baseline 638명, 합계 983명이 재분류 이전엔 정상 백필됐는데 이후엔 안 됨. PR #3이 2026-08-17 main에 머지됨(`62eb88f`) — 후속 수정 필요 여부·`_backfill_group_a` 조건 수정 방향 팀 결정 필요 | `작업기록/jungjinmo/20260817-Global피처엔지니어링-확인필요3건-검증.md`, `code/pipeline/build_person_period.py` |
| 2026-08-16 | choi-1110 | **`past_job_count`가 "첫 일자리·현재 일자리·동시 일자리(부업)"를 아예 세지 않는 정의 문제** — 설문 원문(통합설문지_0227.pdf 194쪽, `d501` 문항) 확인 결과 `past_job_count`의 원천 문항(`d501/d502`, `d001/d002`)이 "앞에서 응답한 첫 일자리, 현재 일자리, 동시 일자리(부업)를 **제외한**" 경험 일자리만 센다. 8/14에 이미 이렇게 구현된 부분이라 이번 세션에서 새로 만든 문제는 아니지만, feature 이름("과거 일한 횟수")과 실제 의미("그 외 추가 경험 일자리 수")가 달라 재정의하거나 문서화가 필요함 | `code/pipeline/source_adapter.py`, `작업기록/choi-1110/20260816-그룹별결측보완-구현.md` |
| ~~2026-08-16~~ | ~~choi-1110~~ | ~~자격증·직업훈련·시험준비·경험일자리 개수형 4개의 "연도별 신규 발생분" 해석이 코드북 응답 패턴 추정~~ → **확인됨 (2026-08-16)**: 통합설문지_0227.pdf 원문으로 직접 확인. `e301`(227쪽)·`e101`(220쪽)·`e001`(216쪽) 모두 "[2차 조사] 조사 시점의 경험과 과거의 경험을 모두 응답 / [3차 조사 이후] 지난 조사 이후의 경험만 응답"이라고 명시돼 있고, `d001`(148쪽)은 "지난 조사와 현재 조사 사이에 일한 일자리", `d501`(194쪽, 1차 전용)은 "한번이라도" 가진 적 있는 일자리 전체를 묻는 회고형임을 확인 — `build_person_period.py`의 baseline까지 연도별 합산 로직이 이 구조(2차=전체이력, 3차+=증분)와 정확히 맞아떨어져 **코드 수정 불필요**. 다만 `past_job_count`·`past_work_months`를 `JOBWAVE`/`JOBSEQ` 기반 직업력 로스터로 정확히 재구성하려면 그 데이터 파일이 필요한데, 코드북만 있고 실제 데이터가 없다는 점은 여전히 미해결 | `code/pipeline/build_person_period.py`, `작업기록/choi-1110/20260816-그룹별결측보완-구현.md` |
| 2026-08-16 | jungjinmo | **"247명" 중복 표기 정정 필요** — v1.2(구직조건 포함) 기준 폐기값. 현재 v1.3 기준 정확치는 Global 2021∩2023=3,185명(2개 이상 등장 합쳐 4,816명) / Local 2021∩2023=304명(2개 이상 등장 합쳐 1,504명). `작업기록/choi-1110/20260814-...` 문서 정정 여부 팀 확인 필요 | `작업기록/choi-1110/20260814-글로벌모델-전처리-설계.md` |
| 2026-08-16 | jungjinmo | **3구간(2021/2022/2023 baseline) 모두 등장하는 사람의 과대표집 처리 방침 미확정** — 삭제하지 않는다는 원칙만 있고 구체 처리(가중치·플래그 등)는 없음. 재분석 결과 Global 4,816명(2개 이상 등장) 중 87%(1,631명)는 파이프라인 구조상 결정되는 반복(라벨 0 재확인)이라 새 정보가 아니고, 독립적인 신규 정보(취업→재실직 이력)를 담은 건 238명뿐 — 우려의 실제 크기를 이 구분에 맞춰 논의할 것 | `작업기록/jungjinmo/20260816-과대표집-결측치-코드북재대조.md` |
| ~~2026-08-16~~ | ~~choi-1110~~ | ~~`build_person_period.py`의 적격 대상자 판정 로직이 브랜치 간 다르게 갈라짐~~ → **해결됨 (2026-08-16)**: choi-1110이 13번 설계안(§1, "다음연도 ECOACT∈{1,2,3}일 때만 행을 만든다")을 근거로 main(hanliyagi) 쪽 `target_ecoact.isin([1,2,3])` 필터를 채택하기로 확정 → `code/pipeline/build_person_period.py`·`plan/details/06-인터페이스.md`·`plan/details/11-...프로토콜.md`를 main 기준으로 병합(`feat/choi-1110-cert-training-workexp-features` 브랜치, 아직 main 미반영). **hanliyagi 본인 확인은 아직 안 됨 — PR 머지 전 최종 확인 필요** | `code/pipeline/build_person_period.py`, `작업기록/choi-1110/20260816-build_person_period-로직충돌-해결.md` |
| ~~2026-08-15~~ | ~~choi-1110~~ | ~~`major_group`(전공계열) 결측 사유가 `Missing`이 맞는지 확정~~ → **해결됨 (2026-08-16)**: 통합설문지 PDF 47쪽(문1)에서 `a413`이 "대학교 또는 대학원에 재학 중인 경우"만 응답하는 문항임을 확인. `student_type=NotApplicable`(현재 재학 중 아님)인 사람과 `major_group` 결측이 3733/4716명(79%)으로 크게 겹치는 것을 데이터로 교차검증해, 그 경우 `NotApplicable`로 재분류하도록 `source_adapter.py` 수정 → 결측 3751명 중 3733명이 `NotApplicable`로 정리되고 진짜 `Missing`은 18명만 남음. 원문 근거 + 데이터 상관관계가 다 확인돼 사람 확인 없이 바로 반영(choi-1110의 "어떻게 하면 해결할 수 있을까" 질문에 대한 답으로 진행) | `code/pipeline/source_adapter.py`, `plan/details/12-Global-피처엔지니어링-요약.md` |
| ~~2026-08-15~~ | ~~choi-1110~~ | ~~`has_major_related_certificate`(전공 관련 자격증) 임계값 확정~~ → **해결됨 (2026-08-16)**: AI가 전공 관련도 문항(`e307_1`)의 정확한 척도 문구(1=전혀 없다~4=매우 관련 깊다)와 "3점 이상 77%(941/1215명) vs 4점만 43%(520/1215명)" 비율 차이를 근거로 제시 → **choi-1110이 검토해 3점 이상을 "관련 있음"으로 확정**. choi-1110 지시대로 "AI가 판단했다"는 점과 근거를 코드 주석·12번 문서에 명시함 | `code/pipeline/source_adapter.py`, `plan/details/12-Global-피처엔지니어링-요약.md` |
| ~~2026-08-15~~ | ~~choi-1110~~ | ~~`school_work_experience`·`past_work_months` 설계 한계 검토~~ → **`school_work_experience`만 해결됨 (2026-08-16)**: 게이팅 문항을 코드북('A(학교생활)' 시트)에서 직접 찾음 — 2~4차 `a172`, 1차 `a601`("재학 중 일자리 경험 여부", 1=있다/2=없다). "없다"를 논리적 0으로 채우도록 수정해 결측 97.9%→81.3%로 감소(choi-1110이 "확실하면 코드 수정해줘"로 적용 지시). **`past_work_months`는 여전히 미해결** — 이 게이트와 무관한 별개 문항군(`d503`/`d532`/`d577`)을 쓰고, `JOBWAVE`/`JOBSEQ` 로스터 데이터가 있어야 정확히 재구성 가능한데 그 파일이 없음 | `code/pipeline/source_adapter.py`, `plan/details/12-Global-피처엔지니어링-요약.md` |
| ~~2026-08-15~~ | ~~choi-1110~~ | ~~모델링 단계 sklearn nullable-dtype 에러 수정~~ → **해결됨 (2026-08-16)**: 팀원 모델링 착수 전 확인 과정에서 이 버그가 `--run-modeling` 명령을 그대로 막는다는 걸 직접 재현으로 확인 → choi-1110 확정 지시로 전처리팀 스코프 밖이지만 바로 고침. `build_features.py`에서 숫자형은 `pd.to_numeric(...).astype("float64")`, 범주형은 `.astype("string").astype(object)` 후 `pd.NA`를 `np.nan`으로 치환하도록 수정, `build_preprocessor().fit_transform()` 정상 동작 확인 | `code/preprocess/build_features.py`, `작업기록/choi-1110/20260816-전체피처-결측분포점검.md` |
| ~~2026-08-15~~ | ~~choi-1110~~ | ~~새 카카오톡 지침("YP2021 Global/Local 데이터셋 전처리 업무안내")이 현재 구현과 정면 충돌~~ → **해결됨 (2026-08-15)**: choi-1110이 새 지침대로 확정 지시 → `build_person_period.py`의 구직/취업준비 필터 삭제 후 재실행, Global 14,906행/7,143명·Local 4,603행/2,838명(전환 1,633/유지 2,970)으로 지침 목표치와 정확히 일치 확인. 상세: `작업기록/choi-1110/20260815-분석대상-필터-제거.md` | `plan/details/11-YP2021-공통-전처리-모델링-프로토콜.md`, `code/pipeline/build_person_period.py` |
| ~~2026-08-13~~ | ~~Kim ByungKyu~~ | ~~YP2021 공통 전처리·평가의 세부 규칙 확정~~ → **해결됨 (2026-08-14)**: 사용자 제공 프로토콜 v1.2를 `plan/details/11-YP2021-공통-전처리-모델링-프로토콜.md`와 `code/config/`에 반영 | `plan/details/11-YP2021-공통-전처리-모델링-프로토콜.md` |
| ~~2026-08-13~~ | ~~Kim ByungKyu~~ | ~~희망직군 6개 KECO 매핑 확정~~ → **해결됨 (2026-08-14)**: KECO 대분류 0~9 → 6개 직군 매핑을 프로토콜 기준으로 `code/config/keco_mapping.csv`에 고정 | `code/config/keco_mapping.csv` |
| 2026-08-13 | Kim ByungKyu | **표본 30명 설문 구성·모집 방식 확정** — 8/16 13:00 문항 제작 회의에서 설문 문항과 표본 모집 방식을 정해야 함 | `회의기록/20260813-전처리-모델링-역할분담.md` |
| 2026-08-09 | NaHoYeol | **실험 설계 결정** — ① 사용할 척도 확정(1순위 조규판 2008 취업불안척도, 원문 확보가 실험 시작 조건) ② 사전 측정을 2회로 할지(2주 전·1주 전 — 설문 1회 추가로 시간·검사 효과를 상당 부분 배제 가능) ③ 표본 수(n=10이면 통계 검정력 없음) ④ 프로그램 출력값(적합도 점수·부족 항목) 저장 방식 — 안 남기면 핵심 분석 불가 | `plan/details/07-연구설계-선행연구맵.md` §5 |
| 2026-08-09 | NaHoYeol | 취업 적합성 판단에 **구직자 심리 변수(취업불안·자신감)를 넣을지** — 넣으려면 설문을 직접 돌려야 하는데 8/11 기획서·8/19 모델링 일정에 맞는지 판단 필요. 쓸 수 있는 국내 타당화 척도 4편은 조사 완료 | `검색기록/20260809-NaHoYeol-청년구직심리-척도.md` |
| 2026-08-09 | NaHoYeol | **07 실험(사전-사후 설문, 10명)을 기획서 범위에 넣을지** — 넣으면 "예측만 한 게 아니라 사람에게 도움이 됐는지까지 봤다"가 발표 차별점이 됨. 모델 완성(8/16) 이후 붙이면 8/24 발표까지 가능 | `plan/details/07-연구설계-선행연구맵.md` §0-2 |
| ~~2026-08-08~~ | ~~hanliyagi~~ | ~~구직자 취업 적합성 판단의 평가 기준과 데이터 출처 확정~~ → **해결됨 (2026-08-09)**: 청년패널2021(YP2021)로 확정 | `plan/details/08-기획서-20260811.md` |
| 2026-08-08 | hanliyagi | 데이터 확보가 실패할 때 전환할 대안 주제와 전환 시점 확정 | `회의기록/20260808-프로젝트-주제-재검토.md` |
| 2026-08-08 | hanliyagi | 아이디어 피칭의 발표 자료 형식과 피칭 후 주제 변경 가능 여부를 팀장에게 확인 | `회의기록/20260808-프로젝트-주제-재검토.md` |
| 2026-08-05 | NaHoYeol | "안전"의 정답 라벨 정의 — 라벨 없으면 지도학습 불가, 비지도로 전환해야 함 | `plan/details/04-구현계획-20260729.md` §1.3 |
| 2026-08-05 | NaHoYeol | `code/` 모듈 분담 — 폴더별 주인 정하기 (preprocess/model/routing/web) | `code/README.md` |
| 2026-08-05 | NaHoYeol | 대상 구역 확정 (종로구 제안 상태) | `plan/details/04-구현계획-20260729.md` |
| 2026-08-05 | NaHoYeol | khuda-data 관리자에게 main 브랜치 보호(PR 필수) 설정 요청할지 | `AGENTS.md` §8 |
| 2026-08-05 | hanliyagi | `hanliyagi` 계정으로 GitHub 원격 저장소 푸시가 403 권한 오류로 실패함 — 저장소 쓰기 권한 부여 또는 올바른 계정·원격 주소 확인 필요 | `회의기록/20260805-로컬-ai-구동-학습.md` |
| 2026-08-05 | NaHoYeol | 로컬 환경에서 AI 구동 방법을 공유함 | `회의기록/안건.md` |
