# 팀 환경 활용 설명서 — 고민이 생기면 여기부터

> 팀 프로젝트를 하다 보면 반드시 만나는 고민들 — "버전이 어긋나면?", "충돌이 났는데?", "오늘 뭘 하지?" — 에 대한 답 모음이다.
> 설치·첫 실행은 [`온보딩.md`](온보딩.md), AI 지시 요령은 [`AI활용팁.md`](AI활용팁.md), 규칙 전문은 [`AGENTS.md`](AGENTS.md).
> 이 문서는 그 사이, **"이럴 땐 어떻게 하지?"**를 담당한다.

---

## 0. 먼저 알기 — 이 환경이 알아서 해주는 것

우리 레포는 어떤 터미널 AI를 켜도 팀 규칙이 자동으로 로드되고, 번거로운 절차는 AI가 대신하게 세팅되어 있다. **외울 것이 거의 없다.**

| 여러분이 하는 것 | AI가 알아서 하는 것 |
|---|---|
| 레포 폴더에서 AI를 켠다 | 팀 규칙(`AGENTS.md`) 로드 · `git pull` · 안전장치(훅) 확인 |
| 일을 시킨다 | 공용 코드(`code/`)면 브랜치 자동 생성 · 시각 산출물이면 `DESIGN-apple.md` 자동 적용 |
| 무언가 찾아달라고 한다 | 검색어·후보·채택/제외 이유를 `검색기록/`에 자동 기록 |
| "오늘 여기까지" 라고 말한다 | 작업기록 정리 → diff 보여주기 → 커밋·푸쉬 (wrap 절차) |
| 막힌 문제를 이야기한다 | `회의기록/안건.md`에 자동 등록 → 다음 회의 안건이 됨 |

---

## 1. 왜 깃으로 소통하나요? — 이 방식의 좋은 점

"카톡으로 파일 주고받으면 되지 않나?" 싶을 수 있어요. 그런데 써보면 이런 게 달라집니다.

1. **"최신본이 어느 거지?"가 사라져요.** 파일을 채팅으로 주고받으면 `최종_진짜최종_수정2.zip`이 반드시 생깁니다. git에서는 원격 저장소(GitHub) 하나가 항상 유일한 최신본이고, 모두가 거기서 받아서 거기에 올려요. "누구한테 최신 파일 있어요?"라는 질문 자체가 없어집니다.
2. **모든 작업에 이력이 남아요.** 누가, 언제, 무엇을, 왜 바꿨는지가 커밋마다 기록됩니다. 채팅은 흘러가서 못 찾지만 git 기록은 쌓여요. 2주 뒤에 "그때 왜 이렇게 정했더라?"가 궁금하면 커밋 로그와 작업기록을 열면 됩니다.
3. **실수해도 되돌릴 수 있어요.** git은 모든 버전을 보관하니까, 잘못 고쳐도 이전 상태로 복원할 수 있습니다. "괜히 건드렸다가 망가뜨리면 어떡하지"라는 공포 없이 과감하게 실험할 수 있어요.
4. **동시에 일해도 서로를 덮어쓰지 않아요.** 두 사람이 같은 시간에 작업해도, git이 합칠 수 있으면 자동으로 합치고 위험하면 강제로 멈춰 세웁니다. 실제로 오늘도 푸쉬가 한 번 거절됐다가 pull 한 번으로 안전하게 합쳐졌어요 (FAQ Q2).
5. **회의 없이도 진행 상황이 공유돼요.** 각자 편한 시간에 일해도, 커밋 로그와 `작업기록/`만 보면 누가 어디까지 했는지 다 보입니다. "언제 시간 되세요?"를 줄이고, 회의는 정말 결정이 필요한 것만 다루면 됩니다.
6. **AI가 팀의 기억을 통째로 물려받아요.** 지시·결정·검색 과정이 전부 git에 남으니, 새 세션의 AI에게 읽혀서 어제 맥락 그대로 이어갈 수 있습니다. "작업 방향 추천해줘"가 가능한 것도 이 기록 덕분이에요.
7. **끝나면 그대로 포트폴리오가 돼요.** 커밋 이력이 곧 "내가 이 프로젝트에서 무엇을 했는지"의 증빙입니다. 따로 정리할 필요가 없어요.

---

## 2. 자주 만나는 고민 10가지 (FAQ)

### Q1. 팀원들이 각자 푸쉬하면 내 로컬과 버전이 어긋나는데, 동기화는 어떻게 하나? 매일 아침 자동 업데이트라도 해야 하나?

필요 없다. git은 구글 문서처럼 실시간으로 맞춰지는 게 아니라 **`git pull`(받기)과 `git push`(올리기)를 실행하는 순간에만** 맞춰진다. 어긋나 있는 것 자체는 정상이고, 문제가 되는 순간은 **옛날 버전 위에서 새 작업을 시작할 때** 하나뿐이다. 그래서 규칙이 "작업 시작 전 pull, 끝나면 push"이고, AI가 세션을 켤 때 pull을 자동으로 해준다. 정해진 시각의 자동 업데이트는 받아봤자 곧 다시 뒤처지므로 의미가 없다.

### Q2. 푸쉬했더니 `rejected — fetch first`라고 거절당했다. 뭔가 망가진 건가?

정상 동작이다. "네가 모르는 작업이 원격에 올라와 있다"는 신호일 뿐이다. `git pull`로 합친 뒤 다시 push하면 끝. git이 이렇게 강제로 멈춰 세워주기 때문에 **모르고 남의 작업을 덮어쓰는 사고는 구조적으로 일어나지 않는다.**

### Q3. 충돌(conflict)이 났다고 한다. 무섭다.

충돌은 **같은 파일의 같은 부분을 두 사람이 다르게 고쳤을 때만** 생긴다. 파일 안에 `<<<<<<<` ~ `>>>>>>>` 로 두 버전이 나란히 표시되니, **어느 쪽을 살릴지는 사람이 읽고 결정**하고 AI에게는 그 결정의 적용만 시킨다. 어느 쪽이 맞는지 모르겠으면 그 코드를 쓴 팀원에게 물어보면 된다. AI에게 "충돌 통째로 해결해줘"는 금물 (§8).

### Q4. 같은 파일을 두 명이 동시에 고치게 될까 봐 걱정된다.

폴더 설계가 이미 막고 있다 — 작업기록·sandbox는 **사람마다 자기 폴더**, 공용 코드는 **모듈 폴더마다 주인 1명**. 서로 다른 파일을 만지는 한 git이 자동으로 합쳐준다. 불가피하게 같은 파일을 만져야 하면 팀 채팅에 먼저 선언하고 시작한다.

### Q5. 브랜치·PR(Pull Request)이 뭔지 모른다.

몰라도 된다. `code/`를 만지는 순간 AI가 브랜치를 만들어 작업하고, 끝나면 PR 링크까지 준다. 여러분은 **링크 열어서 초록 버튼 누르고, 팀 채팅에 리뷰 요청**만 하면 된다. 문서(md)는 그런 절차 없이 main에 바로 올라간다.

### Q6. 내가 만든 파일은 어디에 둬야 하나?

- 실험·연습·개인 중간 산출물 → `sandbox/<내 GitHub 아이디>/` (자유, 리뷰 없이 푸쉬 가능)
- 검증돼서 팀이 쓸 코드 → `code/` (PR로 승격)
- 받아온 원본 데이터 → `data/raw/` (수정 금지, 출처 기록)
- 통합 코드가 만든 팀 최종 결과물 → `data/result/`
- 코드가 지나가는 경로는 **전부 영문**으로 (한글 경로에서 도구가 깨진다, §7)

### Q7. 오늘 뭘 해야 할지 모르겠다.

AI에게 이렇게 물어보라: **"git에 적혀 있는 내용을 바탕으로, 지금 나에게 필요한 작업 방향을 추천해줘."** (팀장 지침 ⑥) AI가 plan·작업기록·안건함을 읽고 다음 할 일을 제안한다. 직접 보려면 [`plan/README.md`](plan/README.md)의 상태 스냅샷과 [`회의기록/안건.md`](회의기록/안건.md)부터.

### Q8. AI가 "완료했습니다"라는데 믿어도 되나?

실행 증거를 보기 전까지는 미완성으로 취급한다. 커밋 전 `git diff`를 직접 읽고, 파일을 만들었다면 크기와 내용물을 확인한다 — 특히 Windows에서는 **에러 없이 조용히 실패**하는 경우가 잦다 (§7). "실행해서 결과 보여줘"를 습관처럼 붙이자.

### Q9. 회의를 했는데 / 문제에 막혔는데, 어디에 남기나?

- 회의 → `회의기록/YYYYMMDD-주제.md` (템플릿이 폴더 README에 있다). 결정된 것은 `plan/`에도 반영해야 끝.
- 막힌 문제, 팀이 정할 사안 → [`회의기록/안건.md`](회의기록/안건.md)에 한 줄. AI에게 말하면 자동으로 등록해준다.

### Q10. Windows에서 명령이 이상하게 실패한다 / PDF를 올리려는데 막힌다.

- 터미널 함정(PowerShell `&&` 없음, 한글 인코딩, 한글 경로, headless 변환 실패)은 전부 `AGENTS.md` §7에 해결책이 정리되어 있다. AI에게 "§7 확인해서 해줘"라고 하면 된다.
- PDF는 원래 안 올라간다 (`.gitignore`가 차단, §4). **문서는 md로** 커밋하고, PDF가 필요하면 로컬에서 만들어 카톡으로 공유한다.

---

## 3. 상황별 치트시트

| 상황 | 이렇게 하면 된다 |
|---|---|
| 작업을 시작한다 | 레포 폴더에서 AI 실행 (pull은 자동). 이어서 할 거면 "작업기록/내아이디/파일 읽고 이어서 해줘" |
| 작업을 끝낸다 | "오늘 여기까지" — 작업기록·커밋·푸쉬가 자동으로 정리된다 |
| 뭘 할지 모르겠다 | "git 내용을 바탕으로 작업 방향 추천해줘" |
| 막혔다 | 같은 명령 반복 대신, 시도한 것 정리시켜서 팀 채팅 + 안건함 |
| 회의가 끝났다 | 회의록 작성 → 결정 사항 `plan/` 반영 → 다룬 안건은 안건함에서 삭제 |
| 자료를 찾아야 한다 | AI에게 시키면 `검색기록/`에 자동 기록. 찾기 전에 기존 기록에서 중복 확인 |
| 파일이 어딨는지 모르겠다 | 루트 [`README.md`](README.md)의 파일 지도 |

---

## 4. 이것만은 사람이 직접 (AI에게 넘기지 않는 것)

1. **diff 읽고 커밋 승인** — 이해 못 한 변경은 커밋하지 않는다.
2. **충돌에서 어느 쪽을 살릴지 결정.**
3. **PR 리뷰와 머지 버튼.**
4. **위험 명령 승인** — `push --force`, `reset --hard`, `rm -rf`가 보이면 멈추고 팀장 확인 (§3).
5. **API 키·비밀번호 관리** — 커밋도, AI 프롬프트에 붙여넣기도 금지 (§3).

---

*더 깊이: 규칙 전문 [`AGENTS.md`](AGENTS.md) · 협업 절차 [`plan/details/05-협업-통합절차.md`](plan/details/05-협업-통합절차.md) · 팀장 지침 [`회의기록/20260805-팀장지침.md`](회의기록/20260805-팀장지침.md)*
