# 1조 온보딩 안내 — 처음 오신 분은 이것만 보면 됩니다

> 대상: KHUDA 26-2 10기 1조 팀원. 처음부터 끝까지 15분이면 됩니다.
> 막히면 팀 채팅에 바로 질문하세요. 혼자 30분 이상 헤매지 않기.

---

## 1. 준비물 설치 (이미 있으면 건너뛰기)

1. **Git** — https://git-scm.com/downloads 에서 받아 설치. 설치 중 선택지는 전부 기본값으로 Next 하면 됩니다.
2. **Node.js** — https://nodejs.org 에서 LTS 버전 설치 (아래 AI 도구들을 설치하는 데 필요합니다).
3. **터미널 AI 도구 하나** — 각자 쓰던 것, 구독 있는 것 아무거나 괜찮습니다:

   | 도구 | 설치 명령 (터미널에 입력) |
   |---|---|
   | Claude Code | `npm install -g @anthropic-ai/claude-code` |
   | Gemini CLI | `npm install -g @google/gemini-cli` |
   | Codex CLI | `npm install -g @openai/codex` |

4. **GitHub 계정** — 없으면 https://github.com 에서 가입 후, **팀장(나호열)에게 아이디를 알려주세요** (레포 권한 초대).

## 2. 레포 받기

터미널을 열고 (Windows는 Git 설치 시 같이 깔린 **Git Bash** 추천):

```
git clone https://github.com/khuda-data/10th-toy-team1.git
cd 10th-toy-team1
git config core.hooksPath scripts/githooks
```

마지막 줄은 팀 자동화를 켜는 설정입니다 — ① 세션 기록을 깜빡한 채 커밋하면 막아주고 ② **업데이트 요약 없이 푸시하면 막아주고** ③ **`git pull` 할 때 팀 업데이트 요약을 자동으로 보여줍니다.** 깜빡하고 안 해도 괜찮습니다 — AI가 세션을 시작할 때 대신 설정해주게 되어 있어요.

## 3. 첫 실행 — AI가 팀 규칙을 아는지 확인

레포 폴더 안에서 AI 도구를 실행하고(예: `claude`, `gemini`, `codex` 입력) 이렇게 물어보세요:

> **"이 레포의 작업 원칙을 요약해봐"**

AI가 "PDF는 커밋하지 않는다, 디자인은 DESIGN-apple.md를 따른다, 세션이 끝나면 작업기록을 남긴다…" 같은 내용을 요약하면 성공입니다. 우리 레포는 어떤 AI를 켜도 팀 규칙(`AGENTS.md`)이 자동으로 로드되게 세팅되어 있어요.

요약을 못 하면 이렇게 지시하세요: *"루트의 AGENTS.md를 읽고 시작해."*

## 4. 꼭 기억할 6가지

0. ⭐ **가장 중요 — 판단은 사람이 씁니다.** 코드 짜기, 문서 서식, 파일 변환 같은 작업은 AI에게 맡기고 너무 붙잡지 마세요. 하지만 **왜 이 논문을 인용하는지, 왜 이 문항을 골랐는지, 왜 설문 대상자를 이렇게 정했는지, 왜 이 방법론인지, 결과를 왜 이렇게 해석했는지**는 **반드시 본인이 직접 써야 합니다.** AI에게 "이런 식으로 볼 수 있다"는 예시를 받는 건 괜찮지만, **그 예시를 왜 골랐는지·자기 해석·자기 시선은 사람이 씁니다.** 문서마다 하단에 「🖊 작성 출처」 표가 붙어 있으니, `⬜ 미검토`가 달린 곳은 본인이 읽고 다시 쓰세요. (전문: [`AGENTS.md`](AGENTS.md) 맨 위 대원칙)
1. **작업 시작 전 `git pull`, 끝나면 push** — 항상 최신 위에서 작업합니다. 풀 받으면 **팀 업데이트 요약이 터미널에 자동으로 뜹니다** (놓쳤으면 루트 [`업데이트.md`](업데이트.md)).
2. **AI가 만든 변경은 `git diff`로 직접 읽고 커밋** — 이해 못 한 변경은 커밋하지 않습니다.
3. **PDF는 올리지 않습니다** — 문서는 md로. (AI가 규칙을 알고 있어서 알아서 지켜줍니다.)
4. **브랜치는 몰라도 됩니다** — 팀 공용 코드(`code/`)를 만지면 AI가 알아서 브랜치를 만들고, 끝나면 PR 링크까지 안내합니다. 여러분은 링크 열어서 버튼 누르고 팀 채팅에 리뷰 요청만. 문서(md)는 그런 절차 없이 main에 바로 올라갑니다.
5. **세션 기록·업데이트 요약은 자동입니다** — AI가 여러분의 지시와 작업 내용을 `작업기록/<본인 GitHub 아이디>/`에 남기고, 남이 알아야 할 변경이면 [`업데이트.md`](업데이트.md)에 요약까지 써서 커밋에 포함합니다. 혹시 빠뜨리면 **커밋·푸시가 자동으로 막히면서** 뭘 하면 되는지 안내가 뜹니다.

## 5. 모르겠으면

- 파일이 어디 있는지 모르겠다 → 루트 [`README.md`](README.md)의 **파일 지도**
- 규칙 전문이 궁금하다 → [`AGENTS.md`](AGENTS.md) (앞부분 "빠른 라우팅" 표부터)
- 프로젝트가 지금 어디까지 왔는지 → [`plan/README.md`](plan/README.md)
- AI한테 시키는 게 서툴다 → [`AI활용팁.md`](AI활용팁.md) (지시 요령 + `/wrap` 같은 팀 명령)
- 그래도 모르겠다 → 팀 채팅에 질문. 질문도 기여입니다.
