# AGENTS.md — 1조 공용 AI 작업 원칙

> KHUDA 26-2 10기 **1조** 토이프로젝트(서울 안전 경로 추천 시스템) 레포에서
> 터미널 AI 도구(Claude Code, Gemini CLI, Codex CLI 등)를 쓸 때 **항상 지켜야 하는 규칙**이다.
> 팀원이 먼저 읽고, 각자의 AI에게도 읽힌다. **규칙은 이 파일 하나에만 적는다** — `CLAUDE.md`·`GEMINI.md`는 이 파일을 가리키는 포인터일 뿐이므로 거기에 규칙을 추가하지 말 것.

---

## 0. 어떤 AI를 쓰든 — 컨텍스트 파일 매핑

각 도구는 레포 루트에서 자기 이름의 파일을 자동으로 읽는다. 이 레포는 전부 `AGENTS.md` 하나로 모이게 세팅돼 있다.

| 도구 | 자동으로 읽는 파일 | 이 레포에서의 동작 |
|---|---|---|
| Claude Code | `CLAUDE.md` | `@AGENTS.md` import → 이 파일이 로드됨 |
| Gemini CLI | `GEMINI.md` | `@AGENTS.md` import → 이 파일이 로드됨 |
| OpenAI Codex CLI | `AGENTS.md` | 이 파일을 직접 읽음 |
| 그 외 (Cursor, Copilot CLI 등) | 도구마다 다름 | 세션 첫 메시지에 "루트의 AGENTS.md를 읽고 시작해"라고 직접 지시 |

세션을 시작하면 AI가 이 파일을 읽었는지 확인하라. "이 레포의 작업 원칙을 요약해봐"라고 물어보면 된다.

---

## 빠른 라우팅 — 무슨 일로 왔는가

이 파일 전체를 다 읽을 필요 없다. 하려는 일에 맞는 곳부터 본다.

| 하려는 일 | 먼저 볼 곳 |
|---|---|
| 처음 왔다 / 파일이 어딨는지 모르겠다 | 루트 `README.md`의 **파일 지도** |
| 프로젝트가 뭐고, 뭐가 결정됐나 | `plan/README.md` → `plan/details/01-overview.md` |
| git 사용·브랜치·PR | §2, §8 |
| 여러 명의 결과물 합치기 / 협업 작업 시작 | §8 → `plan/details/05-협업-통합절차.md` + `06-인터페이스.md` |
| 문서 만들기 (PDF 금지 규칙) | §4 |
| 앱·PPT·대시보드 등 눈에 보이는 것 | §5 → `design/DESIGN-apple.md` |
| 논문 한국어 해설 | `plan/details/02-한국어해설-작성지침.md` → `design/DESIGN-notion.md` |
| 세션 끝내기 (기록 남기기) | §9 → `작업기록/README.md` |
| 막혔다 / 팀이 정해야 할 문제다 | `회의기록/안건.md`에 한 줄 추가 (§2-7) |
| 지침(규칙) 자체를 고치려 한다 | §10 연동 체크 — 다른 문서에 미치는 영향 확인 필수 |
| Windows에서 명령이 이상하게 실패한다 | §7 |
| 하면 안 되는 것 | §3 |
| AI 답변·문서의 말투와 설명 방식 | §11 |
| AI를 더 잘 쓰는 요령·팀 슬래시 명령 | `AI활용팁.md` (명령은 몰라도 자동 적용 — §12) |

---

## 1. 레포 구조 — AI에게 알려줄 지도

```
/  (레포 루트 = 1조 토이프로젝트 그 자체)
├── README.md                ← ★ 파일 지도. "이게 어디 있지?"는 여기서 찾는다 (§10)
├── AGENTS.md / CLAUDE.md / GEMINI.md   ← AI 작업 원칙 (이 파일 + 포인터 2개)
├── plan/                    ← 결정·지침·작업 로그. ★ 프로젝트의 진실의 원천
│   ├── README.md            ← 프로젝트 메타 + 상태 스냅샷 (여기부터 읽기)
│   ├── CHANGELOG.md         ← 날짜순 작업 로그
│   └── details/             ← 주제별 상세 (개요·해설 지침·논문 인덱스·구현계획)
├── design/                  ← 디자인 토큰: DESIGN-apple.md(시각 산출물 전부, §5) · DESIGN-notion.md(해설 전용)
├── 논문/                    ← 논문 1편 = 전용 폴더 1개 (원본 PDF + [해설] html/md)
├── 작업기록/                ← AI 세션 로그 — AI가 자동 기록 (§9)
├── 회의기록/                ← 안건함(안건.md — 막힌 것·정할 것 즉시 추가) + 회의록 (§2-7)
├── code/                    ← 전체를 하나로 모은 통합 코드 (모듈 폴더마다 주인 1명, §8)
├── data/                    ← raw/(원본, 수정 금지·출처 기록) · result/(팀 공용·최종 결과물만)
├── sandbox/                 ← 각자 개인 작업 공간: sandbox/<git아이디>/ (개인 중간 산출물 포함, §8)
└── scripts/                 ← 유틸 스크립트 + git 훅 (작업기록 누락 커밋 차단)
```

- 산출물과 plan 문서가 충돌하면 **산출물이 진실** — plan을 갱신한다.
- 결정한 것은 `plan/details/`에, 언제 무엇을 했는지는 `plan/CHANGELOG.md`에 남긴다. AI에게 작업을 시켰으면 로그 갱신까지가 한 세트다.

---

## 2. 세션 루틴 (git)

1. **작업 시작 전 `git pull`.** 남의 최신 작업 위에서 시작한다.
2. **커밋 전 `git status` + `git diff`.** AI가 만든 변경은 반드시 사람이 읽는다. **이해하지 못한 변경은 커밋하지 않는다.**
3. 커밋 메시지는 한 줄 요약(한국어 가능). 서로 다른 작업을 한 커밋에 섞지 않는다.
4. 세션이 끝나면 **`작업기록/`에 세션 로그를 남기고**(§9) push. 로컬에만 둔 채 며칠 묵히지 않는다.
5. **문서(md)는 `main` 직접 푸쉬 허용. 코드는 브랜치 + PR** — 규칙은 §8.
6. **최초 1회 설정**: clone 직후 `git config core.hooksPath scripts/githooks` — 작업기록 없는 커밋을 막아주는 안전장치(§9)를 켠다. **AI 규칙: 세션 시작 후 첫 git 작업 전에 `git config core.hooksPath` 값을 확인하고, 비어 있으면 조용히 설정하라.**
7. **막힌 문제·팀이 정해야 할 사안은 `회의기록/안건.md`에 즉시 한 줄 추가.** **AI 규칙: 세션 중 해결하지 못한 막힘이나 팀 결정이 필요한 사안(형식 합의, 분담, 데이터 선택 등)이 나오면, 사용자가 시키지 않아도 안건함에 추가하라.** 회의에서 결정되면 `plan/`에 반영한다.

---

## 3. 절대 금지 — AI가 제안해도 실행 전에 멈추고 팀장 확인

- `git push --force`, `git reset --hard`, `git checkout -- .`, `rm -rf` 등 **되돌릴 수 없는 명령**
- **다른 팀원 파일의 임의 수정·"정리"** — 내 작업 범위 밖 파일은 건드리지 않는다
- API 키·토큰·비밀번호를 **커밋하거나 AI 프롬프트에 붙여넣는 것** (`.env`, `*.key`는 .gitignore에 이미 있다)
- 원본 자료(논문 PDF 등) 삭제·이동
- AI가 위 명령을 "필요하다"고 제안하는 경우가 실제로 있다. 그 자리에서 실행하지 말고 이유를 물어본 뒤 팀 채팅에 공유.

---

## 4. 문서 규칙 — PDF는 커밋하지 않는다

- **레포에 올리는 문서는 전부 Markdown(md)이다** (2026-08-05 결정). `.gitignore`가 `*.pdf`를 막는다. **예외는 논문 원본 PDF 하나** — 출처 자료라 커밋을 유지한다.
- 이미 PDF로 만든 문서는 md로 변환해 올린다:
  ```python
  # pip install pymupdf4llm
  import pymupdf4llm
  md = pymupdf4llm.to_markdown("문서.pdf", write_images=True,
                               image_path="assets", image_format="png", dpi=150)
  open("문서.md", "w", encoding="utf-8").write(md)
  ```
- **한글·공백·대괄호가 든 파일명은 md 속 이미지 링크를 깨뜨린다** → PDF를 ASCII 이름의 임시 사본(예: `plan01.pdf`)으로 복사해 변환하고, md만 원래 이름으로 저장한다.
- PDF가 필요하면(발표·인쇄) 로컬에서 생성해 카톡 등으로 공유한다. 레포에는 올리지 않는다.

---

## 5. 디자인 규칙 — 시각 산출물은 무조건 `DESIGN-apple.md`

**앱 UI, 웹 프론트, PPT·슬라이드, 대시보드, 포스터, 데모 화면 등 눈에 보이는 것을 만들 때는 예외 없이 `design/DESIGN-apple.md`를 따른다.**

- AI에게 시각 산출물을 시키면 AI는 **작업 시작 전에 `DESIGN-apple.md`를 먼저 읽고**, 그 안의 토큰(색·타이포·간격·컴포넌트 규칙)**만** 사용해야 한다. 즉석에서 새 색·새 폰트·새 디자인을 창작하지 않는다.
- 팀원이 디자인을 하나도 몰라도 된다 — "앱 만들어줘"라고만 해도 AI가 이 규칙에 따라 `DESIGN-apple.md`대로 만들게 되어 있다. AI가 다른 디자인을 제안하면 이 파일을 가리키며 거부하라.
- **유일한 예외**: 논문 한국어 해설 HTML은 `design/DESIGN-notion.md`를 따른다 (`plan/details/02-한국어해설-작성지침.md`에 고정된 기존 규칙).
- 두 파일 모두 **수정 금지**. 토큰이 부족하면 임의로 추가하지 말고 팀장에게 요청.

---

## 6. AI에게 일 시키는 방식

- **큰 변경은 "설명 먼저 → 확인 → 실행".** AI가 추측으로 계속 진행하게 두지 않는다.
- AI 답변에 **근거를 요구한다** — `파일명:라인` 또는 plan 문서명. "그냥 그렇다"는 답은 다시 묻는다.
- AI가 "완료했다"고 말해도 **직접 실행·확인 전에는 완료가 아니다.** 산출물의 최종 책임은 사람에게 있다.
- 막히면 같은 명령을 반복시키지 말고, 무엇을 시도했고 뭐가 안 됐는지 정리해서 팀 채팅에 공유한다.

---

## 7. Windows 터미널 함정 (전부 실제로 겪은 것)

팀원 대부분이 Windows라는 전제. AI가 Unix 습관대로 낸 명령이 아래에서 깨진다.

| 함정 | 해결 |
|---|---|
| PowerShell 5.1에는 `&&` / `\|\|`가 없다 (파서 에러) | `A; if ($?) { B }` 로 잇거나 Git Bash 사용 |
| 파이썬에서 한글/특수문자를 콘솔에 print → `UnicodeEncodeError` (cp949) | 콘솔에 찍지 말고 **UTF-8 파일로 덤프한 뒤 읽는다**. 파일 쓸 때 `encoding="utf-8"` 명시 |
| 한글·공백 경로에서 도구가 깨진다 (Edge headless 변환 실패, `gradlew test` 실패 등) | **ASCII 임시 경로를 경유**해 작업하고 결과물만 원래 위치로 복사 |
| headless Chrome/Edge를 PowerShell `&` 연산자로 실행하면 **조용히 실패** (파일 미생성, 에러도 없음) | `Start-Process -Wait -PassThru` 로 실행 |
| HTML→PDF 변환 시 구식 플래그가 말없이 무시됨 | `--headless=new` (구식 `--headless`는 파일 미생성 가능) + `--no-pdf-header-footer` (구식 `--print-to-pdf-no-header`는 무시됨). 변환 후 **파일 크기·페이지 수 확인** |
| CSV로 한글 데이터를 주고받으면 엑셀에서 깨진다 (cp949↔UTF-8) | **xlsx 사용** — 내부가 UTF-8 XML이라 구조적으로 안 깨진다 |

공통 원칙: **AI가 "됐다"고 한 파일 생성·변환은 크기와 내용물을 직접 확인한다.** 조용한 실패가 Windows에서 특히 잦다.

---

## 8. 코드 협업 — 브랜치·작업 공간·병합

### 브랜치 (GitHub Flow)

1. `main`은 항상 동작하는 상태를 유지한다. **코드는 main에 직접 푸쉬 금지** (문서 md만 직푸시 허용).
2. 작업 시작: `git switch -c feat/<git아이디>-<주제>` (예: `feat/NaHoYeol-risk-model`).
3. 끝나면 push → GitHub에서 PR 생성 → **팀원 1명 이상 리뷰 후 머지**. 리뷰 없는 셀프 머지 금지.
4. 브랜치는 작게, 수명 짧게. 며칠씩 끌수록 병합 충돌이 눈덩이가 된다.
5. PR 올리기 전 `git pull origin main`으로 최신 main을 내 브랜치에 합쳐, 충돌은 **내 로컬에서 내가** 해결한 상태로 올린다.

**AI 규칙 — 브랜치 자동 운전 (팀원이 브랜치 개념을 몰라도 되게, 시키지 않아도 수행):**

- 작업 대상이 `code/` 등 공용 코드면, 사용자가 브랜치를 언급하지 않아도 **작업 시작 전에 `feat/<git아이디>-<주제>` 브랜치를 만들어 이동**하고 한 줄로만 알린다 ("공용 코드라 브랜치를 만들어 작업할게요").
- 문서(md)·자기 `sandbox/` 작업은 브랜치 없이 main에서 바로 진행한다.
- **세션 시작 시 `git branch`로 현재 위치를 확인하라.** 지난 세션의 브랜치에 남아 있으면 상태를 쉽게 설명하고, 이번 작업이 문서/sandbox면 main 복귀부터 한다.
- 공용 코드 작업이 끝나면: push → 터미널에 출력되는 **PR 생성 링크를 사용자에게 주고**, "링크 열어서 초록 버튼(Create pull request) 누르고, 팀 채팅에 리뷰 요청하세요"까지 안내한다. **리뷰와 머지 버튼은 사람이 누른다.**
- 브랜치 이동이 막히거나(커밋 안 한 변경) 충돌이 나면, 무슨 상황인지 §11 스타일로 쉽게 설명하고 위 "충돌이 났을 때" 절차를 따른다.

### 작업 공간 — 충돌은 병합 기술이 아니라 폴더 설계로 예방한다

- 공용 코드는 전부 `code/` 아래, 모듈 폴더로 나누고 **폴더마다 주인 1명** (예: `code/preprocess/`, `code/model/`, `code/routing/`, `code/web/` — 실제 분담은 팀 회의로). 남의 폴더는 PR로만 수정.
- 데이터는 `data/raw/`(원본 — 수정 금지, 출처 기록 필수)와 `data/result/`(가공 결과물 — 재생성 가능해야 함)로 구분. 10MB 초과 파일은 커밋하지 않는다 (`data/README.md`).
- 개인 실험·연습·**개인 중간 산출물**은 전부 `sandbox/<git아이디>/` 아래 자유 (자기 폴더 = 자기 마음대로, 리뷰 없이 푸쉬 가능). `data/result/`에는 통합 코드가 만든 팀 공용·최종 결과물만 넣는다. 검증된 코드만 `code/`로 승격한다 (PR).
- **코드 경로는 전부 ASCII(영문)로 만든다** — 한글 경로에서 도구가 깨진다 (§7).
- 같은 파일을 두 사람이 동시에 고치는 상황 자체를 만들지 않는 것이 제1원칙. 불가피하면 팀 채팅에 먼저 선언하고 시작.

### 합치는 순서 — 뼈대 먼저, 약속 먼저 (웹·분석 공통)

여러 명의 결과물을 합치는 것은 마지막 기술이 아니라 첫날의 설계다. 4단계: **① 돌아가는 뼈대를 main에 먼저 ② 모듈 사이 약속(API·데이터 형식)을 `plan/details/06-인터페이스.md`에 기록 ③ 각자 자기 폴더 + 브랜치 ④ 작은 PR로 자주 합치고 합친 main을 실행해 확인.** 상세 절차와 체크리스트: `plan/details/05-협업-통합절차.md`.

**AI 규칙**: 공용 코드(`code/`)·공용 데이터(`data/`) 작업을 시작하면 ⓐ `06-인터페이스.md`를 먼저 읽고 거기 적힌 형식대로 구현하라. ⓑ 필요한 약속이 아직 없으면 임의로 정하지 말고 문서에 추가하는 것부터 제안하라. ⓒ 뼈대가 없으면 뼈대 만들기부터 제안하라. ⓓ 상대 파트가 미완성이면 약속된 형식의 가짜(mock) 데이터로 개발을 진행하라.

### 충돌이 났을 때

AI에게 "충돌 해결해줘"라고 통째로 맡기지 않는다. `git status`로 충돌 파일 확인 → 파일 안의 `<<<<<<<` ~ `>>>>>>>` 블록을 **사람이 읽고** 어느 쪽을 살릴지(또는 어떻게 합칠지) 결정 → AI에게는 그 결정을 말한 뒤 적용만 시킨다. 어느 쪽이 맞는지 모르겠으면 그 코드를 쓴 팀원에게 물어본다.

---

## 9. AI 세션 기록 — 지침과 답변도 버전 관리한다

AI에게 **무엇을 시켰고 무엇을 받았는지는 코드만큼 중요한 산출물**이다. 기록 위치: `작업기록/<git아이디>/YYYYMMDD-<주제>.md`.

**이 기록은 팀원이 직접 쓰는 것이 아니라 AI가 자동으로 남긴다. AI 규칙 (사용자가 시키지 않아도 수행):**

1. 세션에서 파일을 만들거나 고치는 작업이 시작되면, **그 시점에 오늘 날짜의 세션 로그 파일을 만들고** 사용자의 지시를 요지 그대로 기록하라. 세션이 진행되는 동안 계속 이어 적는다.
2. **커밋할 때마다 로그를 최신 상태로 갱신해 함께 스테이징하라.** 로그 없는 커밋은 pre-commit 훅이 차단한다 (`scripts/githooks/pre-commit`).
3. **개인 폴더명은 항상 그 사람의 GitHub 계정 이름(git 아이디)이다** (예: `NaHoYeol`) — 작업기록·sandbox·브랜치 이름 모두 동일. 모르면 이 순서로 알아내라: ① 기존 `작업기록/`·`sandbox/` 하위 폴더 ② `git config user.email`이 noreply 주소(`숫자+아이디@users.noreply.github.com`)면 그 아이디 ③ 그래도 불확실하면 **사용자에게 GitHub 아이디를 한 번만 물어서** 확정.
4. 훅이 설정 안 된 클론이면(§2-6) 조용히 설정하라. 사용자가 "기록 빼고 커밋해"라고 명시할 때만 `SKIP_LOG=1`을 쓴다.

- 템플릿과 실제 예시는 `작업기록/README.md` 참조. 핵심 4항목: **① 내가 준 지침(프롬프트 요지) ② AI가 한 일(변경 파일 목록) ③ 내가 검증한 방법과 결과 ④ 남은 것·막힌 것**.
- AI 답변 전문을 다 붙일 필요는 없다 — 재현에 필요한 지침과 결정만. 단 **잘 먹힌 중요한 프롬프트는 원문 그대로** 남긴다 (다음에 같은 작업을 시킬 때 재사용).
- 도구별 내보내기 기능(Claude Code `/export` 등)이 있지만 도구마다 제각각이라, **팀 표준은 이 md 기록**이다.
- 이 기록이 있으면: 다음 세션에서 AI에게 읽혀 이어서 작업할 수 있고, "누가 뭘 어디까지 했나"가 팀에 공유되고, 잘못된 결정이 언제 들어왔는지 역추적할 수 있다.

---

## 10. 문서·파일이 늘어날 때 — 지도 유지와 지침 분산

이 파일 하나에 모든 걸 쌓으면 아무도 못 찾는다. 늘어나는 것은 아래 두 장치로 흡수한다.

### 파일 지도 = 루트 `README.md`

- 레포의 모든 폴더·중요 파일은 루트 `README.md`의 **파일 지도** 표에 한 줄씩 등록한다. "이게 어디 있지?"는 항상 거기서 시작한다.
- **AI 규칙: 새 폴더나 중요한 산출물(데이터셋, 분석 노트북, 모델 파일, 앱 모듈 등)을 만들면, 같은 커밋에서 `README.md` 지도에 한 줄 추가한다.** 사용자가 시키지 않아도 자동으로 한다. 지도에 없는 파일은 팀에게 없는 파일이나 마찬가지다.
- **AI 규칙: 새 폴더를 만들 때는 반드시 그 폴더 안에 `README.md`를 함께 만든다.** 내용 3가지 — ① **무엇**이 들어가는 폴더인가 ② **왜** 만들었나(의도 — 이게 없으면 나중에 아무도 못 지운다) ③ **규칙**(있다면). 처음 보는 팀원이 그 README만 읽고 이해할 수 있게 쓴다. 기존 폴더의 README들이 견본이다.
- 파일이 많아지는 폴더(분석 결과, 데이터 등)는 그 폴더 안에 자체 `README.md`를 만들어 상세 목록을 옮기고, 루트 지도에는 폴더 한 줄만 남긴다 (`작업기록/`이 이 패턴의 예시).

### 지침을 고칠 때 — 연동 체크 (항상, 기본)

같은 규칙이 여러 문서에 언급되어 있다. 한 곳만 고치면 나머지는 **거짓말이 된다.**

**AI 규칙 (지침·규칙이 담긴 문서를 수정할 때마다, 시키지 않아도 수행):**

1. 바뀐 규칙의 **핵심 단어·경로·섹션 번호로 레포 전체를 검색**해, 같은 내용을 언급하는 곳을 전부 찾는다.
2. 영향받는 곳을 **같은 커밋에서 함께 고친다.** 같은 규칙이 두 곳에 다르게 적혀 있으면 그건 규칙이 두 개인 것이다 — 반드시 한 커밋 안에서 일치시킨다.
3. 어디를 확인했고 어디를 고쳤는지 세션 로그(작업기록)에 남긴다.
4. **지침의 버전 관리**: 의미가 바뀌는 수정(오타 제외)은 푸쉬 후 팀 채팅에 한 줄 공유한다. 큰 변경이 쌓이면 팀장이 **git 태그**(v1.1, v2.0 …)로 스냅샷을 찍는다. 기준선: **v1.0 "팀장 환경 세팅"** (2026-08-05). 특정 버전과의 차이는 `git diff v1.0 -- AGENTS.md`, 복원은 팀장과 상의 후.

**자주 얽히는 곳 (연동 지도):**

| 이 문서를 고치면 | 함께 확인할 곳 |
|---|---|
| `AGENTS.md`의 규칙·섹션 번호 | 빠른 라우팅 표(§ 번호 참조) · 루트 `README.md` 규칙 요약 · `온보딩.md` · `plan/README.md` 고정 컨벤션 · 각 폴더 README · `.gitignore` 주석 |
| 폴더 구조·경로 | `AGENTS.md` §1 지도 · 루트 `README.md` 파일 지도 · 문서 속 상대경로 링크 전부 (grep으로 옛 경로 검색) |
| 폴더별 README의 규칙 | 루트 `AGENTS.md`의 해당 § · 루트 `README.md` 지도의 한 줄 설명 |
| `plan/` 결정 | `plan/CHANGELOG.md`(기록) · 관련 details 문서 · 회의에서 나온 것이면 `회의기록/` |

### 지침 분산 = 폴더별 `AGENTS.md`

- 루트의 이 파일에는 **모두에게 항상 적용되는 규칙만** 남긴다.
- 특정 폴더에만 해당하는 지침(데이터 전처리 규칙, 앱 코드 컨벤션, 실험 재현 절차 등)이 생기면 **그 폴더 안의 `AGENTS.md`**에 쓰고, 위 "빠른 라우팅" 표에 한 줄 등록한다. AI는 그 폴더에서 작업할 때 해당 파일을 먼저 읽는다.
- 현재 등록된 폴더 지침: 아직 없음 (생기면 여기와 빠른 라우팅에 추가).

---

## 11. AI 산출물 스타일 — 모든 생성물 공통 지시

어떤 AI를 쓰든, 이 레포에서 만들어지는 **모든 것** — 답변, 문서, 코드 주석, 커밋 메시지, 세션 로그 — 은 아래 스타일을 따른다. 읽는 사람은 "이 분야를 처음 접하는 팀원"이라고 가정하고 쓴다.

1. **자연스러운 말투로 쓴다.** 번역투("~에 대하여 수행되었다")나 딱딱한 기계 문어체 대신, 옆자리 팀원에게 설명해주듯 쓴다. 기본 언어는 한국어 (코드 식별자·라이브러리명·명령어는 원어 그대로).
2. **이해하기 쉬운 용어를 쓴다.** 전문용어·약어는 처음 등장할 때 한 번 풀어서 쓴다 — 예: "임베딩(문장을 서로 비교할 수 있는 숫자 목록으로 바꾼 것)". 설명 없이 약어를 던지지 않는다.
3. **구체적으로 설명한다.** "적절히 처리했다", "일부 수정했다" 같은 뭉뚱그림 금지. 무엇을(파일·함수), 어떻게(방법·명령), 왜(근거) 했는지 명시하고, 셀 수 있는 것은 숫자로 말한다 ("문서 3개에서 표기 9곳 수정").
4. **쉽게 쓰되 장황하게 쓰지 않는다.** 스타일을 지키느라 분량을 늘리지 않는다 — 같은 내용이면 짧은 쪽이 좋은 쪽이다.

---

## 12. 스킬 자동 적용 — 명령어를 몰라도 되게

`.claude/commands/`의 팀 절차들은 슬래시 명령으로 부를 수도 있지만, **몰라도 된다.**

**AI 규칙: 아래 상황이 감지되면, 사용자가 명령어를 치지 않아도 해당 절차 파일을 읽고 그대로 수행하라** (어떤 AI 도구든 — 절차 파일은 그냥 md라서 누구나 읽을 수 있다):

| 이런 상황이 감지되면 | 자동 적용할 절차 |
|---|---|
| 사용자가 "커밋해줘", "푸쉬해줘", "오늘 여기까지", "마무리하자" 등 세션을 끝내려 한다 | `.claude/commands/wrap.md` (작업기록 → 연동 체크 → 지도 확인 → diff 승인 → 커밋·푸쉬) |
| 논문을 주며 해설·번역·정리를 요청한다 | `.claude/commands/haeseol.md` (02-지침 파이프라인) |
| 해결 못 한 막힘이 남았거나, 팀이 정해야 할 사안이 나왔다 | `.claude/commands/agenda.md` (안건함 등록 — §2-7과 동일) |

- 적용할 때는 한 줄로만 알린다 (예: "세션 마무리 절차를 적용할게요"). 절차 내용을 사용자에게 설명하라고 요구하지 않는다.
- 새 절차 파일을 만들면 **이 표와 `AI활용팁.md` §3 표에 함께 등록**한다 (§10 연동 체크).
