# AI 활용 팁 — 터미널 AI를 더 잘 쓰는 법

> 규칙이 아니라 **요령** 모음이다. 규칙은 `AGENTS.md`, 처음 설치는 `온보딩.md`.

---

## 1. 지시 잘하는 법 5가지

1. **구체적으로.** "분석해줘" ❌ → "data/raw/생활인구.csv에서 시간대별 평균을 구해서 그래프로 만들어줘" ⭕. 파일명·원하는 결과·형식을 말할수록 정확해진다.
2. **파일을 직접 지목하라.** "아까 그 코드" 대신 `sandbox/mina/pop_analysis.py`처럼. AI는 경로를 주면 헤매지 않는다.
3. **큰 일은 계획부터 시키라.** "바로 하지 말고, 어떻게 할 건지 계획 먼저 보여줘" — 방향이 틀렸으면 코드가 나오기 전에 잡을 수 있다.
4. **검증을 같이 시키라.** "만들고 나서 실행해서 결과 보여줘", "정말 되는지 확인해줘" — AI의 "됐습니다"는 실행 증거가 있을 때만 믿는다.
5. **이어서 작업할 땐 기록을 읽히라.** 새 세션 첫마디: "작업기록/mina/20260812-pop-analysis.md 읽고 이어서 해줘" — 어제의 맥락이 그대로 복원된다.

## 2. 도구 공통 요령

- **중간에 멈추기**: 대부분 `ESC`. AI가 이상한 방향으로 가면 바로 끊고 다시 지시하는 게 싸다.
- **대화가 길어지면 새 세션.** 세션이 길어질수록 AI가 앞부분을 잊거나 느려진다. 작업기록을 남기고 새로 시작 → 기록 읽혀서 이어가기.
- **위험한 명령은 승인 창이 뜬다** (파일 삭제, 인터넷 요청 등). 무슨 명령인지 읽고 승인할 것 — 이해 안 되면 "이거 뭐 하는 명령이야?"라고 물어보면 된다.
- **스크린샷·이미지 붙여넣기**: 에러 화면, 디자인 참고 이미지를 그대로 붙여넣으면 AI가 읽는다 (세 도구 모두 지원).

## 3. 팀 슬래시 명령 (이 레포에 내장 — Claude Code)

레포의 `.claude/commands/`에 커밋해둔 팀 공용 명령. Claude Code에서 `/`를 입력하면 자동으로 뜬다:

| 명령 | 하는 일 |
|---|---|
| `/wrap` | **세션 마무리** — 작업기록 정리 → 연동 체크 → 파일 지도 확인 → diff 보여주고 커밋·푸쉬 |
| `/agenda 안건내용` | 회의 안건함(`회의기록/안건.md`)에 한 줄 추가 |
| `/haeseol 논문경로` | 논문 한국어 해설 제작 (02-지침 파이프라인 실행) |

새 명령 만들기: `.claude/commands/이름.md` 파일 하나 = 명령 하나. 파일 안에 "AI에게 시킬 프롬프트"를 적고 커밋하면 팀 전체에 공유된다. (Gemini/Codex 사용자는 그 md 파일 내용을 복사해 붙여넣으면 같은 효과.)

## 4. 도구별 알아두면 좋은 것

버전에 따라 다르니, 정확한 목록은 각 도구에서 `/help`로 확인.

**Claude Code**
- `Shift+Tab` 두 번 = **플랜 모드**: 파일을 건드리지 않고 계획만 세우게 함. 큰 작업 전에 켜면 좋다.
- `claude --continue`: 직전 세션 이어가기 / `claude --resume`: 지난 세션 골라서 이어가기
- `/export`: 대화 전체를 파일로 내보내기 (작업기록의 원본 증빙으로 좋음)
- `/compact`: 대화가 길어져 무거울 때 요약해서 계속
- `!명령어`: AI를 거치지 않고 셸 명령 바로 실행 (예: `!git status`)

**Gemini CLI** — `/chat save`·`/chat resume`(세션 저장·복원), `/memory show`(로드된 컨텍스트 확인 — AGENTS.md가 보이면 정상), `/stats`(토큰 사용량)

**Codex CLI** — `/diff`(지금까지의 변경 확인), 승인 모드 조절(자동 실행 범위) — 처음엔 승인 요구 모드 권장

## 5. 이런 건 AI에게 시키지 말 것

- **이해 안 되는 diff를 "그냥 커밋해줘"** — §2-2 위반이기 전에, 사고의 지름길이다.
- **충돌 통째로 해결** — 어느 쪽 코드를 살릴지는 사람이 정한다 (§8).
- **API 키 붙여넣기** — 절대 금지 (§3).
- 같은 실패를 3번 이상 반복시키기 — 멈추고 "지금까지 뭘 시도했고 뭐가 안 됐는지 정리해줘" → 그걸 들고 팀 채팅 또는 `/agenda`.
