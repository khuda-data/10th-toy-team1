# 업데이트 알림 — `git pull` 받으면 여기부터

> **최신이 맨 위.** 작업한 사람이 자기 결과물을 한 항목으로 요약해 올린다 (AI가 자동으로 작성해 준다 — `AGENTS.md` §13).
> `git pull` 하면 **새로 들어온 항목이 터미널에 자동으로 뜬다** (`scripts/githooks/post-merge`).
> 터미널에서 놓쳤으면 이 파일을 위에서부터 읽으면 된다.

**쓰는 법 (읽는 사람 기준으로 3줄)**
- **무엇이 바뀌었나** — 파일 이름 말고, 뭐가 달라졌는지
- **팀원이 할 일** — 없으면 "없음"
- **자세히** — 문서·커밋 링크

---

## 2026-08-20 · hanliyagi — PR #6 split 수치 감사와 Global·Local 공식 모델 범위 정리

**무엇이 바뀌었나**
- 보관된 두 `split_ids.csv`는 SHA-256과 모든 SAMPID 배정이 같고, 두 Global Dataset에도 같은 14,906개 Person-Period 키가 있음을 확인했습니다. 따라서 현재 고정 split의 재현 가능한 행 수는 Train 11,925 / Test 2,981입니다. split을 새로 선택하거나 교체하지 않았습니다.
- Global과 Local의 공식 비교 모델을 모두 Logistic Regression·XGBoost로 `official_comparison_models`에 명시했습니다. Decision Tree·Random Forest·LightGBM 구현과 설정은 삭제하지 않았습니다.
- 수정 뒤 Stage 0 Notebook을 실제 baseline 42개 Feature Dataset에서 다시 실행해 42개 Feature 누락 0, SAMPID overlap 0과 세 그래프 생성을 확인했습니다.

**팀원이 할 일**
- PR #6 작업기록의 12,012 / 2,894행 표기를 정정할지는 해당 기록 작성자 또는 팀이 보관 산출물 기준으로 결정해 주세요. Stage 1 진행 여부와 결과 해석은 계속 사람이 판단합니다.

**자세히**: [`작업기록`](작업기록/hanliyagi/20260820-Global-모델링-준비구현.md) · [`공통 프로토콜`](plan/details/11-YP2021-공통-전처리-모델링-프로토콜.md) · [`Stage 0 Notebook`](notebooks/global/00_modeling_check.ipynb)

---

## 2026-08-20 · hanliyagi — Global 모델 Stage 0 점검 Notebook + LR/XGBoost 공용 기능 준비

**무엇이 바뀌었나**
- 공식 Global·Local 비교 대상은 Logistic Regression과 XGBoost 두 모델로 설정했습니다. 기존 Decision Tree·Random Forest·LightGBM 코드와 설정은 지우지 않았지만, 공식 비교 흐름과 Notebook에는 쓰지 않습니다.
- XGBoost는 각 CV Train fold의 negative/positive 비율로 `scale_pos_weight`를 계산합니다. OOF 예측, SAMPID 1,000회 bootstrap 95% CI, 저장 Dataset/split 로더도 공용 코드에 추가했습니다.
- `notebooks/global/00_modeling_check.ipynb`는 실제 42개 Feature Global Dataset과 고정 split의 표본·Target·baseline_year·결측률을 표와 세 그래프로 확인합니다. 모델 학습·선택·결과 해석은 하지 않습니다.

**팀원이 할 일**
- Stage 1 전에 `00_modeling_check.ipynb`를 실행해 결과를 직접 확인하고, 다음 단계로 넘어갈지 사람 판단을 남겨 주세요. Jupyter를 저장소 밖에서 시작했다면 `KHUDA_PROJECT_ROOT`에 저장소 경로를 지정해야 합니다.

**자세히**: [`Stage 0 Notebook`](notebooks/global/00_modeling_check.ipynb) · [`공용 코드 안내`](code/README.md) · [`세션 로그`](작업기록/hanliyagi/20260820-Global-모델링-준비구현.md)

---

## 2026-08-20 · hanliyagi — Global 모델을 0~4단계·사람 판단 절차로 고정

**무엇이 바뀌었나**
- Global 모델링을 한 번에 끝내지 않고, ① 42개 Feature의 LR/XGBoost 1차 CV, ② 중요도·공선성 분석 후 사람의 Feature 선택, ③ 선택 Feature 2차 CV와 사람의 후보 판단, ④ 네 후보의 고정 Test 비교와 최종 판단으로 진행하도록 공용 절차를 만들었습니다.
- `.py`는 재사용 계산·학습·평가, `.ipynb`는 실행·표·그래프·관찰, 사람은 해석·Feature 선택·최종 모델 결정을 담당합니다. Test는 마지막 단계 전까지 선택에 사용하지 않습니다.

**팀원이 할 일**
- Global 모델링을 시작하거나 다음 단계로 넘어가기 전 [14번 실행 흐름](plan/details/14-Global-모델링-단계별-실행흐름.md)을 확인하세요. Stage 1·2·3·4 사이의 선택과 해석은 담당자가 직접 기록해야 합니다.

**자세히**: [`단계별 실행 흐름`](plan/details/14-Global-모델링-단계별-실행흐름.md) · [`세션 로그`](작업기록/hanliyagi/20260820-Global-모델링-단계별-프로토콜.md)

---

## 2026-08-19 · NaHoYeol — 취업 스트레스 설문지 초안 (한글 파일) + `설문/` 폴더 신설

**무엇이 바뀌었나**
- 사전·사후 실험에 쓸 **설문지 초안을 만들었습니다.** 김고운 외(2023) 한국 대학생 취업 스트레스 척도 22문항입니다. 문항·채점법·출처·제작 경위가 md에 다 들어 있습니다.
- 경희대 IRB 양식(KHGIRB v6.2) 그대로 만든 **배포용 한글 파일(.hwp)도 있지만 레포에는 올리지 않았습니다** (§4 — 문서는 md로 커밋). 인쇄용 hwp가 필요하면 NaHoYeol에게 요청하세요.
- 22문항이 논문 원문과 글자 그대로 일치하는지 기계 대조했습니다 — **22/22 일치**. `plan/details/07-...md` 부록 A-4에 "⬜ 원문 대조 권장"으로 남아 있던 상태였습니다.
- 원문을 다시 뒤지다 **신뢰도 α가 표본에 따라 다르다**는 걸 확인했습니다. 07 문서에 적힌 전체 α = .92는 **예비조사(202명 EFA)** 기준이고, **본조사(438명 CFA) 기준은 .94**입니다. 둘 다 논문에 있는 값이라 07이 틀린 건 아니지만, 인용할 때 표본을 안 밝히면 오해를 삽니다. 설문지에 두 값을 표로 병기했습니다.
- `설문/` 폴더를 새로 만들고 참고용 원본 양식(다른 연구의 SUS·SAS·인터뷰 설문지)도 함께 뒀습니다.

**팀원이 할 일**
- **이 설문지는 아직 배포하면 안 됩니다.** "왜 이 척도를 골랐는가"가 `07 문서` §5-1-1에서 아직 `⬜ AI 제안 · 미검토` 상태이고, 설문지 전체가 그 위에 서 있습니다. 대원칙상 문항 선택은 사람이 판단해야 하는 영역이라, NaHoYeol이 근거를 쓴 뒤에야 쓸 수 있습니다.
- 설문 실험을 할지 말지, 대상자·표본 수를 어떻게 할지는 8/9부터 안건함에 그대로 있습니다. **모델링 일정과 겹치므로 회의에서 한 번 정리가 필요합니다.**
- 취업 스트레스 척도를 인용할 계획이 있으면 위의 α 표본 구분을 확인해 주세요.

**자세히**: [`설문지`](설문/취업스트레스-설문지-20260819.md) · [`설문 폴더 안내`](설문/README.md) · [`안건함`](회의기록/안건.md)

---

## 2026-08-19 · wjdwlsah — Global→Local Train/Test 분할 실데이터 검증 (Windows 크래시 수정 포함)

**무엇이 바뀌었나**
- `--run-modeling` 완료 안내 print가 Windows 콘솔(cp949)에서 em dash 문자 때문에 `UnicodeEncodeError`로 죽던 문제를 고쳤습니다(`code/pipeline/run_pipeline.py`). 산출물 저장은 이미 끝난 뒤라 실질 피해는 없었지만 스크립트가 실패한 것처럼 보였던 문제입니다.
- **① Global 분할**: 실제 YP2021 raw 데이터로 Global Train/Test 분할(§15, `StratifiedGroupKFold` 5-fold 중 1-fold 고정 test)을 재실행해 **Train 5,737명·Test 1,406명(80.3%/19.7%, 행 기준 12,012/2,894)**, 사람 단위로 겹침 0명임을 확인했습니다.
- **② Local 분할**: Local 6개 직군 데이터셋이 ①의 Global 분할을 SAMPID 기준으로 그대로 상속받아, **Global test에 속한 사람이 어느 직군에서도 Local train에 섞여 들어가는 일이 없음**을 확인했습니다(6개 직군 개별 점검 포함 리키지 0건). 6개 직군 train 비율은 77.1%~81.9%(사람 기준)로 목표 80:20 근처에 분포합니다.
- 분할 로직 자체는 코드 변경 없음 — 기존 `split.py`/`select_split()` 구조가 이미 이 요구사항(같은 비율 + 리키지 없음)을 만족하도록 짜여 있었고, 이번엔 raw 데이터로 실측 재확인한 것입니다.

**팀원이 할 일**
- **PR 리뷰 부탁드립니다.** ①②를 하나의 PR로 묶었습니다(커밋 2개: Windows 수정+Global 검증 → Local 검증 순서).

**자세히**: [`Global 세션 로그`](작업기록/wjdwlsah/20260819-Global-train-test-분할-실데이터검증.md) · [`Local 세션 로그`](작업기록/wjdwlsah/20260819-Local-train-test-분할-실데이터검증.md)

---

## 2026-08-19 · hanliyagi — PR #5를 최신 main에 맞추고 실험별 결과 보관 규칙 정리

**무엇이 바뀌었나**
- PR #4를 PR #5 브랜치에 병합했습니다. 충돌했던 알림·안건 문서는 양쪽 기록을 모두 보존했고, `major_group` 백필 수정과 `n_prior_periods` 추가가 함께 적용된 상태를 실제 원자료로 확인했습니다.
- 실험별 `datasets/*.parquet`, `splits/split_ids.csv`, `models/*.joblib`가 Git에 올라가지 않도록 `.gitignore`를 새 `data/result/<experiment>/` 경로에 맞췄습니다. `data/result/README.md`도 세 실험 폴더와 재실행 방법으로 갱신했습니다.
- YP2021 Excel을 읽는 데 필요한 `openpyxl`을 `code/requirements.txt`에 추가했습니다.

**팀원이 할 일**
- PR #5의 최신 커밋을 확인한 뒤 기존 리뷰 절차에 따라 머지해 주세요. 이번 검증에서 생성한 결과 파일은 로컬 산출물이며, 공유할 지표 파일은 따로 검토해 선택적으로 커밋하면 됩니다.

**자세히**: [`세션 로그`](작업기록/hanliyagi/20260819-PR5-충돌해결-결과경로-정리.md)

---

## 2026-08-18 · wjdwlsah — `n_prior_periods`·`sample_weight`를 독립적으로 켜고 끄도록 분리 + raw 데이터 실 검증

**무엇이 바뀌었나**
- 바로 아래 항목(오버샘플링 완화)에서 `n_prior_periods`(Feature)와 `sample_weight`(학습 가중치)가 항상 같이 켜지는 구조였는데, 이제 서로 독립적으로 켜고 끌 수 있습니다. `build_global_dataset`/`build_local_dataset`에 `use_n_prior_periods`, `run_pipeline._fit_model`(내부적으로 `train_model`/`tune_model`)에 `use_sample_weight` 플래그가 생겼고, 둘 다 기본값은 `False`라 **기존 42개 Feature 모델과 완전히 동일하게 재현**됩니다.
- `features.yaml`에 `n_prior_periods`가 일반 `features:` 목록이 아니라 별도 `optional_features:`에 있어서, 명시적으로 요청하지 않으면 43번째 Feature로 새지 않습니다.
- `run_pipeline.py`에 `--use-n-prior-periods`/`--use-sample-weight`/`--compare-oversampling-mitigation`(raw를 한 번만 읽어 baseline/+n_prior_periods/+sample_weight 세 실험을 순서대로 실행) CLI 옵션을 추가했고, 결과물을 `data/result/<experiment>/` 하위로 완전히 분리해 세 모델의 datasets·metrics·feature_importance·`*.joblib`가 서로 덮어쓰지 않습니다(모델 객체 저장 자체도 이번에 처음 추가).
- **raw 데이터(`YP2021_w01~04.xlsx`)로 실제 검증**: PR #4(`major_group` 백필)를 로컬에서만 합친 임시 브랜치에서, ① baseline_42features의 X가 PR#4 반영 후 순정 42개 파이프라인 출력과 **컬럼·dtype·값까지 완전히 일치**, ② with_n_prior_periods만 정확히 43번째 컬럼(`n_prior_periods`) 1개가 추가, ③ with_sample_weight는 컬럼 구성은 baseline과 동일하되 실제로 `LogisticRegression.fit()`에 가중치가 반영돼 계수가 달라짐(반영 안 됐으면 이상한 것 — 정상적으로 달라짐을 확인), ④ Local Dataset에서도 두 플래그가 올바르게 전달되고 `sample_weight` 사람별 합=1이 항상 보장됨을 확인했습니다.

**팀원이 할 일**
- **PR #4(https://github.com/khuda-data/10th-toy-team1/pull/4) → PR #5(https://github.com/khuda-data/10th-toy-team1/pull/5) 순서로 리뷰·머지 부탁드립니다.** 이번 분리 작업은 PR #5 브랜치(`feat/wjdwlsah-oversampling-mitigation`)에 새 커밋으로 추가돼 있어 PR #5에 자동 반영됩니다.
- 세 실험 비교는 `python -m code.pipeline.run_pipeline --raw-zip <경로> --run-modeling --compare-oversampling-mitigation`로 한 번에 실행할 수 있습니다.

**자세히**: [`세션 로그`](작업기록/wjdwlsah/20260818-오버샘플링-완화-n_prior_periods-sample_weight-구현.md)

---

## 2026-08-18 · wjdwlsah — 오버샘플링 완화: `n_prior_periods` feature + `sample_weight` 가중치 추가

**무엇이 바뀌었나**
- Global Person-Period에서 세 baseline_year 모두 등장하는 2,947명(41.3%)이 전체 행의 59.3%를 차지하는 과대표집 문제에 대응해, `person_period.parquet`에 `n_prior_periods`(같은 사람이 이전에 몇 번 더 미취업으로 관찰됐는지) Feature를 추가하고, `DatasetBundle`에 `sample_weight`(1/그 사람의 행 수, 사람당 가중치 합이 1이 되게 함) 필드를 추가했습니다.
- `sample_weight`는 Global 전체·Local 부분집합·직군별 부분집합, 이렇게 실제로 모델이 보는 데이터셋 단위마다 각각 다시 계산되도록 `build_global.py`/`build_local.py`/`run_pipeline.py`의 `_subset_bundle`을 수정했습니다. 반대로 `n_prior_periods`는 Global 전체 이력 기준으로 한 번만 계산해 Local에 그대로 이어받습니다(희망직업 미응답으로 Local에서 특정 연도 행이 빠져도 이력값이 왜곡되지 않게).
- `tune_model`/`train_model`에 `sample_weight_train` 인자를 추가해 학습(fit)에 반영되게 했습니다. `plan/details/06-인터페이스.md`도 새 스키마·미확정 사항을 반영해 갱신했습니다.

**팀원이 할 일**
- **PR 리뷰 부탁드립니다.** raw 데이터로 `--run-modeling` 재실행해서 `n_prior_periods`/`sample_weight` 실제 값과 파이프라인 전체 경로가 정상 동작하는지 확인이 필요합니다(이 세션엔 원자료가 없어 문법 검증만 했습니다).
- **팀 결정 필요**: `sample_weight`가 학습(fit)에만 반영되고 `GridSearchCV`의 검증 fold 채점(scoring)에는 반영되지 않는데, 이대로 둘지 scoring도 가중치 반영하도록 바꿀지 — 안건함에 등록했습니다.

**자세히**: [`세션 로그`](작업기록/wjdwlsah/20260818-오버샘플링-완화-n_prior_periods-sample_weight-구현.md) · [`06-인터페이스.md`](plan/details/06-인터페이스.md) · [`안건함`](회의기록/안건.md)

---

## 2026-08-18 · wjdwlsah — `major_group` Group A 백필이 `NotApplicable` 재분류와 충돌하는 버그 수정

**무엇이 바뀌었나**
- jungjinmo가 발견한 버그(983명의 과거 전공값이 조용히 유실되는 문제, `업데이트.md` 바로 아래 항목·안건함 참고)를 고쳤습니다. `code/pipeline/build_person_period.py`의 `_backfill_group_a`가 baseline 값이 `NaN`일 때만 과거 연도를 뒤지던 것을, `"NotApplicable"`일 때도 뒤지도록 수정했습니다 — "가장 최근에 확인된 실제 전공을 쓴다"는 정의로 확정했습니다.
- `gender`·`education_level`은 현재 특수결측 규칙상 `NotApplicable`을 만들지 않아 이 수정의 영향을 받지 않는다는 것을 `yp2021_missing_rules.json`으로 확인했습니다.

**팀원이 할 일**
- **PR 리뷰 부탁드립니다.** 특히 raw 데이터로 재실행해서 983명(2022 baseline 345명·2023 baseline 638명) 중 실제로 몇 명이 전공값을 되찾는지 확인이 필요합니다(이 세션 환경엔 원자료가 없어 문법 검증만 했습니다).

**자세히**: [`세션 로그`](작업기록/wjdwlsah/20260818-major_group-백필버그-수정-오버샘플링-논의.md) · [`회의기록/안건.md`](회의기록/안건.md)

---

## 2026-08-17 · jungjinmo — Global 피처엔지니어링 "확인 필요" 3건 통계 검증 + `major_group` 백필 버그 발견

**무엇이 바뀌었나**
- `12-Global-피처엔지니어링-요약.md`의 "확인 필요" 3건을 내가 낸 가설·판단대로 실제 raw 데이터·통계 검정으로 검증했습니다.
- ① `major_group`의 `NotApplicable` 재분류(choi-1110, PR #3)가 Group A 백필(`_backfill_group_a`)과 충돌해 **983명(2022년 baseline 345명·2023년 baseline 638명)의 과거 전공값이 조용히 유실**되는 걸 실제 raw 데이터 재실행으로 확인했습니다. 재분류가 백필보다 먼저 baseline 값을 `"NotApplicable"`로 확정시켜버려서, 백필 진입 조건(`isna()`)이 더 이상 참이 아니게 되는 구조적 문제입니다.
- ② `has_major_related_certificate`의 3점 임계값은 표본(전체 1,845명, 타겟과 연결하면 392명)으로는 3점/4점을 통계적으로 구분할 수 없음(2-표본 z검정 p=0.105, Fisher's exact p=0.121, 80% 검정력 최소 탐지 차이 15.2%p > 실제 관측 차이 8.2%p)을 확인해 현재 기준이 타당함을 뒷받침했습니다.
- ③ `school_work_experience` 게이팅 문항(`a172`/`a601`)을 통합설문지 원문(22쪽/74쪽)으로 독립 교차검증했고, choi-1110의 기존 구현과 문항 코드·라벨이 전부 일치했습니다.

**팀원이 할 일**
- **①은 이미 main에 반영돼 있습니다(PR #3, 2026-08-17 머지 `62eb88f`).** `_backfill_group_a`가 baseline이 `"NotApplicable"`이어도 과거 유효값을 계속 찾도록 후속 수정할지 논의해 주세요. 안건함에 등록했습니다.
- ②·③은 현재 구현(3점 이상 임계값, `a172`/`a601` 게이트) 유지 근거가 보강된 것이라 별도 조치는 필요 없습니다.

**자세히**: [`세션 로그`](작업기록/jungjinmo/20260817-Global피처엔지니어링-확인필요3건-검증.md) · [`안건함`](회의기록/안건.md)

---

## 2026-08-16 · choi-1110 — Global 모델링 착수 차단 버그 수정 + 결측 재분류 3건, PR 리뷰 요청

**무엇이 바뀌었나**
- 팀원이 `code/README.md`의 `--run-modeling` 명령을 그대로 치면 항상 `TypeError: boolean value of NA is ambiguous`로 실패하던 sklearn 버그를 고쳤습니다(`code/preprocess/build_features.py`). 원인은 pandas nullable dtype(`Int64`/`Float64`/`string`)의 결측 마커를 scikit-learn이 못 읽는 것이었습니다.
- `education_level`·`student_status` 두 Feature가 원자료 연도별 dtype 차이 때문에 `"2"`/`"2.0"`처럼 서로 다른 범주로 쪼개져 있던 캐스팅 버그도 같이 고쳤습니다(`code/pipeline/source_adapter.py`).
- 42개 Feature 전체 결측률·분포를 처음으로 점검했고(`plan/details/12-Global-피처엔지니어링-요약.md`), `code/README.md`의 오래된 "Feature 매핑 완성 전엔 모델 실행 못 함" 안내를 42개 완료 기준으로 갱신했습니다.
- 위 수정 후 `--run-modeling` 전체 경로(Global 1개 + Local 6개 직군)가 끝까지 도는 것을 직접 재현으로 확인했습니다(`logistic_regression` 검증, `xgboost`·`lightgbm` 등 나머지 4개 모델은 미검증).
- 코드북·통합설문지 원문을 다시 뒤져서 `plan/details/12-...` 문서의 "확인 필요" 3건 중 2건을 해결했습니다: ① `school_work_experience`가 게이팅 문항(`a172`/`a601`)을 못 찾아 결측 97.9%였던 문제를 원문에서 문항을 찾아 수정(→81.3%), ② `major_group`이 "대학/대학원 재학 중"에게만 묻는 문항임을 설문지에서 확인해, `Missing`으로 뭉쳐 있던 결측 3,751명 중 3,733명을 `NotApplicable`로 재분류했습니다. `has_major_related_certificate` 임계값(3점 이상)은 근거를 코드 주석에 남기고 확정했습니다.

**팀원이 할 일**
- **PR 리뷰 부탁드립니다**: https://github.com/khuda-data/10th-toy-team1/compare/main...feat/choi-1110-cert-training-workexp-features?expand=1 — 특히 `build_features.py`의 dtype 변환 로직과, `source_adapter.py`의 `school_work_experience`·`major_group` 재분류 로직을 봐주세요(둘 다 원문·데이터 근거로 판단 없이 바로 적용한 수정이라 사람 확인이 필요합니다).
- 머지되면 `main`을 pull 받아 `code/README.md`의 명령대로 바로 모델링을 시작할 수 있습니다(각자 로컬에 YP2021 raw zip 필요).

**자세히**: [`세션 로그`](작업기록/choi-1110/20260816-전체피처-결측분포점검.md) · [`12번 문서`](plan/details/12-Global-피처엔지니어링-요약.md) · [`안건함`](회의기록/안건.md)

---

## 2026-08-16 · choi-1110 — hanliyagi ↔ choi-1110 브랜치 간 build_person_period.py 로직 충돌 해결

**무엇이 바뀌었나**
- 어제 등록된 "적격 대상자 판정 로직이 브랜치 간 다르게 갈라짐" 안건을 처리했습니다. 실제로 `main`을 `feat/choi-1110-cert-training-workexp-features`에 병합해보니 **5개 파일**(`code/pipeline/build_person_period.py`, `plan/details/06-인터페이스.md`, `plan/details/11-YP2021-공통-전처리-모델링-프로토콜.md`, `업데이트.md`, `회의기록/안건.md`)에서 충돌이 났습니다.
- 문제의 핵심은 `build_person_period.py`에서 나와 hanliyagi가 같은 날(8/15) 같은 목표로 각자 코드를 고쳤는데, hanliyagi 쪽에만 `target_ecoact.isin([1,2,3])` 검증이 있었다는 점입니다. 이 조건이 없으면 다음연도 응답이 무효할 때도 취업전환 여부가 자동으로 0(비취업)으로 기록될 수 있습니다. 13번 설계안 §1이 이 조건을 명시하고 있어, hanliyagi(main) 쪽 로직을 채택하기로 확정했습니다.
- `06-인터페이스.md`·`11-...프로토콜.md`도 같은 이유로 충돌났는데, main 쪽이 이미 13번 설계안과 일치하는 v1.3 정본이라 main 버전으로 통일했습니다. 내 브랜치가 이 두 문서에 냈던 변경은 main의 재작성에 이미 포함돼 있어 유실된 내용은 없습니다.

**팀원이 할 일**
- **hanliyagi의 최종 확인이 필요합니다.** `target_ecoact` 필터 채택은 choi-1110이 13번 설계안을 근거로 판단한 것이라, PR 머지 전 hanliyagi가 직접 대조해서 이견이 없는지 확인해 주세요. 실제 코드 변경(`build_person_period.py`)은 아직 `feat/choi-1110-cert-training-workexp-features` 브랜치에만 있고 main에는 반영하지 않았습니다.

**자세히**: [`세션 로그`](작업기록/choi-1110/20260816-build_person_period-로직충돌-해결.md) · [`안건함`](회의기록/안건.md)

---

## 2026-08-16 · hanliyagi — Person-Period 전처리 설계안 공유

**무엇이 바뀌었나**
- Person-Period 3개 구간과 42개 Feature의 그룹별 처리 기준을 담은 전처리 설계안을 팀 공용 계획 문서에 추가했습니다. 결측·비해당·논리적 0을 구분하고, 과거 최신값을 쓸 수 있는 Feature와 쓸 수 없는 Feature를 나눠 설명합니다.

**팀원이 할 일**
- 전처리 구현 전 설계안의 Feature 그룹과 처리 원칙을 확인해 주세요. 원변수 매핑·특수결측·설문 분기는 코드북을 대조해 담당자가 검증해야 합니다.

**자세히**: [`전처리 설계안`](plan/details/13-YP2021-PersonPeriod-전처리-설계안.md)

---

## 2026-08-16 · hanliyagi — 데이터셋 구간·결측치 처리 회의록 정정

**무엇이 바뀌었나**
- 데이터셋은 취업 직전 한 시점만 고르는 방식이 아니라, 각 응답자의 2021→2022·2022→2023·2023→2024 세 구간을 모두 사용하도록 회의록을 정정했습니다. 2023년 결측 시 최신 과거 응답 사용, 코드북 대조, 42개 Feature와 Local 데이터셋 6개 생성 방침도 함께 기록했습니다.

**팀원이 할 일**
- 코드 구현 전 회의록을 확인해 주세요. 현재 공통 코드도 세 구간 구조이며, 취업자만 유리한 직전 시점을 고르는 선택 편향을 피하기 위한 결정입니다.

**자세히**: [`회의록`](회의기록/20260816-데이터셋-구간-결측치-피처엔지니어링.md)

---

## 2026-08-16 · choi-1110 — hanliyagi ↔ choi-1110 브랜치 간 build_person_period.py 로직 충돌 해결

**무엇이 바뀌었나**
- `feat/choi-1110-cert-training-workexp-features`를 main에 병합하는 과정에서 `code/pipeline/build_person_period.py`의 적격 대상자 판정 로직이 두 사람 사이에 다르게 갈라져 있던 걸 발견했습니다(둘 다 "구직·취업준비 필터 삭제"를 각자 고쳤는데, hanliyagi의 main(v1.3) 쪽만 `target_ecoact.isin([1,2,3])` 조건이 있었음). 13번 설계안 §1이 이 조건을 명시하고 있어 choi-1110이 main 쪽 로직을 채택하기로 확정했고, 같이 충돌났던 `06-인터페이스.md`·`11-...프로토콜.md`도 main 기준으로 맞췄습니다.

**팀원이 할 일**
- hanliyagi도 이 결정에 이견이 없는지 최종 확인 부탁드립니다.

**자세히**: [`회의기록/안건.md`](회의기록/안건.md)

---

## 2026-08-16 · choi-1110 — Global 피처 엔지니어링 요약 문서 신설

**무엇이 바뀌었나**
- 8/14~15에 구현한 Global 모델 feature 31개(자격증·직업훈련·일경험·전공계열·취업준비노력 등)를 팀원이 코드 안 봐도 이해할 수 있게 `plan/details/12-Global-피처엔지니어링-요약.md`로 정리했습니다. 뭘 만들었는지, 핵심 규칙 2가지, 작업 중 발견한 범주형 결측 처리 버그, 아직 사람 검토가 필요한 판단 지점 3건(`major_group` 결측 사유, 자격증 임계값, 일경험 feature 설계 한계)을 담았습니다.

**팀원이 할 일**
- 확인 필요 지점 3건은 아직 〔AI 제안〕 상태입니다 — `plan/details/12-Global-피처엔지니어링-요약.md`의 "확인 필요" 절 읽고 의견 있으면 회의기록/안건.md에서 논의해 주세요.
- 코드 변경은 아직 `feat/choi-1110-cert-training-workexp-features` 브랜치의 PR에 있고 리뷰 대기 중입니다.

**자세히**: [`plan/details/12-Global-피처엔지니어링-요약.md`](plan/details/12-Global-피처엔지니어링-요약.md)

---

## 2026-08-16 · jungjinmo — 과대표집 규모 재분석 + 결측치 3종 코드북 재대조 완료

**무엇이 바뀌었나**
- `baseline_year` 조합별로 4,816명(2개 이상 등장)을 뜯어봤더니, **87%(1,631명)는 person-period 구조상 자동으로 라벨 0이 확정되는 반복**이고, 진짜 독립적인 신규 정보(취업→재실직 이력)를 담은 건 238명뿐임을 원자료로 확인했습니다.
- `student_type`(46.0%)·`university_type`(38.7%)·`recent_employment_prep`(34.6%) 결측치를 코드북·원자료와 재대조했습니다. 셋 다 실제 결측이 아니라 문항 비해당이며, `university_type`은 `university_type_graduate`(a123) 부분만 이전 차수 값 이월(carry-forward)이 필요합니다(입학 케이스 a308은 이미 처리돼 있음).

**팀원이 할 일**
- 과대표집 처리 방침은 여전히 팀 결정 필요 — 안건함 항목이 이번 재분석 내용으로 갱신됐습니다.
- `student_type`·`recent_employment_prep`의 "비해당" 카테고리 인코딩, `university_type_graduate`(a123)의 carry-forward 로직 구현이 남아있습니다.

**자세히**: [`세션 로그`](작업기록/jungjinmo/20260816-과대표집-결측치-코드북재대조.md) · [`안건함`](회의기록/안건.md)

---

## 2026-08-16 · jungjinmo — 업무안내 대조 결과: Local Feature 31개 미구현 확인, 잔여 테스크 정리

**무엇이 바뀌었나**
- `Toy_project/YP2021_Global_Local_데이터셋_전처리_업무안내.md`의 Local 지침을 실제 코드와 한 줄씩 대조했습니다. Local 고유 로직(희망직업 선택·KECO 매핑)은 지침과 일치하지만, `features.yaml` 42개 Feature 중 자격증·직업훈련·시험준비·일경험·전공 등 **31개는 `source_adapter.py`에 원자료 매핑 코드가 아직 없습니다.** 잔여 테스크를 우선순위별로 정리했습니다.

**팀원이 할 일**
- 코드북 대조로 미구현 31개 Feature의 원자료 문항 코드를 확정해야 `--run-modeling`이 strict 모드로 돌아갑니다. 작업 순서는 아래 자세히의 세션 로그 "남은 것" 절 참고.
- 안건함에 등록한 2건(247명 표기 정정, 3구간 과대표집 처리 방침)은 팀 결정이 필요합니다.

**자세히**: [`세션 로그`](작업기록/jungjinmo/20260816-업무안내대조-잔여테스크-정리.md) · [`features.yaml`](code/config/features.yaml)

---

## 2026-08-15 · choi-1110 — 안건함에 전처리 확인 필요 사항 4건 등록

**무엇이 바뀌었나**
- 오늘 전처리 검증 중 확인된 팀 확인 필요 사항 4건을 `회의기록/안건.md`에 새로 등록했습니다: `major_group` 결측 사유 확정, `has_major_related_certificate` 임계값 확정, `school_work_experience`·`past_work_months` 설계 한계 검토, 모델링 단계 sklearn nullable-dtype 에러 수정(모델 담당자용).

**팀원이 할 일**
- 회의기록/안건.md에서 해당 4건 확인 부탁드립니다.

**자세히**: [`회의기록/안건.md`](회의기록/안건.md)

---

## 2026-08-15 · choi-1110 — 분석 대상 미취업자 전체로 확대 + 범주형 결측/비해당 구분 버그 수정

**무엇이 바뀌었나**
- 새 카카오톡 지침("YP2021 Global/Local 데이터셋 전처리 업무안내")에 따라 `build_person_period.py`에서 구직 중(`c602`)·취업준비 중(`c635`) 조건으로 사람을 거르던 필터를 없앴습니다. 이제 기준연도 `ECOACT`가 실업·비경제활동인 미취업자 **전체**가 분석 대상입니다. 구직·취업준비 여부는 그대로 feature로만 씁니다.
- 재실행 결과 Global Person-Period가 3,278행 → **14,906행(고유 7,143명)**, Local이 2,172행 → **4,603행(고유 2,838명, 취업전환 1,633/미취업유지 2,970)**으로 늘었고, 지침에 적힌 목표 수치와 정확히 일치함을 확인했습니다.
- 범주형 feature(`student_type`, `university_type`, `major_group` 등)에서 "문항 비해당"과 "진짜 결측"이 둘 다 그냥 빈 값으로만 남아 모델 학습 단계에서 전부 `Missing` 하나로 뭉개지던 구현 누락도 같이 고쳤습니다. `student_type`(4,716건)·`university_type`(4,331건)이 이제 실제로 `NotApplicable` 범주로 남습니다.
- `plan/details/11-YP2021-공통-전처리-모델링-프로토콜.md`, `plan/details/06-인터페이스.md`도 새 기준에 맞게 갱신했습니다.

**팀원이 할 일**
- 표본이 3,278행 → 14,906행으로 크게 늘어서, 이전에 좁은 표본 기준으로 확인했던 feature별 결측률·값 분포는 재검증이 필요할 수 있습니다.
- `major_group`의 결측 사유가 `Missing`으로 고정돼 있는 게 맞는지(전공이 없는 사람이 있어 `NotApplicable`이어야 하는 건 아닌지)는 코드북만으로 확정 못 해 그대로 뒀습니다 — 세션 로그에서 확인 후 이견 있으면 알려주세요.
- 이전에 공유했던 sklearn nullable-dtype 모델링 에러는 이번 작업과 무관하게 그대로 남아있습니다.

**자세히**: [`작업기록`](작업기록/choi-1110/20260815-분석대상-필터-제거.md)

---

## 2026-08-15 · hanliyagi — YP2021 문서 번호·구현 상태 동기화

**무엇이 바뀌었나**
- 공통 프로토콜의 하위 제목 번호를 상위 제목과 맞추고, 진행계획의 완료된 Person-Period·Dataset·split 작업을 현재 구현 상태로 고쳤습니다. 진행계획에도 6개 Local 직군별 검증 수치를 반영했습니다.

**팀원이 할 일**
- 없음. 표본 수의 사람 최종 확인 상태는 프로토콜의 검토 상태 표를 그대로 따릅니다.

**자세히**: [`공통 프로토콜`](plan/details/11-YP2021-공통-전처리-모델링-프로토콜.md) · [`진행계획`](plan/details/10-YP2021-직군별-취업예측-계획.md)

---

## 2026-08-14 · choi-1110 — Global 전처리: 나머지 22개 feature 구현 완료(미구현 0개)

**무엇이 바뀌었나**
- 지난 항목(자격증·훈련·일경험 9개)에 이어 나머지 22개도 다 채웠습니다: `major_group`(전공계열), `months_since_graduation`(졸업 후 경과월), 취업노력 유형 `prep_effort_01~12`+`prep_effort_other`, 졸업 전후 취업준비·구직활동 경험 2종, 시험준비 관련 3종. `features.yaml`의 Feature 45개 중 100% 빈칸이던 컬럼이 이제 0개입니다.
- 특히 취업노력 유형(`prep_effort_*`)은 1차 조사가 2~4차와 문항 형식이 달랐는데(순위형 vs 개별 예/아니오), 코드북 대조로 두 형식의 값 순서가 같다는 걸 확인해서 **1차도 채웠습니다.** hanliyagi가 남겨뒀던 "1차와 2~3차 취업노력 문항의 동등성" 이슈가 해소됩니다.
- `school_work_experience`(재학중 일경험), `past_work_months`(총 근무개월)는 코드북에 명확한 근거가 부족해 한계를 명시한 채로 최대한 채웠습니다(값이 매우 희소함).
- **모델링 담당자에게**: `--run-modeling`을 시험 실행해보니 `SimpleImputer`가 pandas nullable 타입(`Int64`/`Float64`)과 충돌해서 실패합니다. `recent_job_search` 등 기존 feature에도 있던 잠재 문제인데, 지금까지는 빈 컬럼이 있어서 이 단계까지 아무도 못 왔던 것 같습니다. 전처리 스코프가 아니라 고치지 않고 로그에만 남겼습니다.

**팀원이 할 일**
- `school_work_experience`·`past_work_months`·`has_major_related_certificate` 임계값 등 AI 제안 부분은 세션 로그에서 확인 후 이견 있으면 알려주세요.
- 모델 담당자는 `build_features.py`에서 숫자형은 `.astype("float64")`, 범주형은 `.astype(object)`로 강제 변환하면 위 sklearn 에러가 풀릴 걸로 보입니다 — 실제 모델링 전에 확인 필요합니다.
- PR(`feat/choi-1110-cert-training-workexp-features`) 리뷰 부탁드립니다.

**자세히**: [`작업기록`](작업기록/choi-1110/20260814-글로벌전처리-자격증훈련일경험-구현.md)

---

## 2026-08-14 · choi-1110 — Global 전처리: 자격증·직업훈련·일경험 feature 9개 구현

**무엇이 바뀌었나**
- `source_adapter.py`가 만드는 Global/Local 공통 Feature 중 자격증·직업훈련·경험일자리 관련 9개(`has_certificate`, `certificate_count`, `has_major_related_certificate`, `has_employment_certificate`, `has_vocational_training`, `vocational_training_count`, `vocational_training_hours`, `ever_worked_before`, `past_job_count`)가 전부 빈칸이던 상태에서 실제 원자료 값으로 채워지도록 구현했습니다. 이 문항들이 1차(2021) 조사엔 없다는 것도 코드북으로 확인해 반영했습니다(2021 기준 행은 구조적으로 결측 처리).
- `has_major_related_certificate`의 "전공 관련 있음" 판정 기준(전공관련도 3점 이상)은 AI 제안이라 팀 검토가 필요합니다.

**팀원이 할 일**
- `has_major_related_certificate` 임계값 기준에 이견 있으면 알려주세요.
- `school_work_experience`(재학중 일경험), `past_work_months`(총 근무개월)는 코드북만으로 결측 사유·계산식을 확정 못 해 이번엔 구현하지 않았습니다. 나머지 21개 미구현 feature(전공 대계열·취업노력 유형·시험준비 등)와 함께 다음 배치로 남아있습니다.

**자세히**: [`작업기록`](작업기록/choi-1110/20260814-글로벌전처리-자격증훈련일경험-구현.md) · 브랜치 `feat/choi-1110-cert-training-workexp-features` (PR 링크는 리뷰 요청 시 팀 채팅에 별도 공유)

---

## 2026-08-14 · hanliyagi — 미취업자 전체 기준 상세 계획·프로토콜 교체

**무엇이 바뀌었나**
- 진행계획은 최신 미취업자 전체 Person-Period 계획으로, 프로토콜은 이전 버전 비교 설명을 뺀 상세 v1.3 실행 정본으로 교체했습니다. 코드·인터페이스·설정·파생 데이터 생성 로직은 바꾸지 않았습니다.

**팀원이 할 일**
- 구현 전 [`진행계획`](plan/details/10-YP2021-직군별-취업예측-계획.md)과 [`공통 프로토콜`](plan/details/11-YP2021-공통-전처리-모델링-프로토콜.md)을 기준으로 작업해 주세요.

**자세히**: [`진행계획`](plan/details/10-YP2021-직군별-취업예측-계획.md) · [`프로토콜`](plan/details/11-YP2021-공통-전처리-모델링-프로토콜.md) · [`인터페이스`](plan/details/06-인터페이스.md)

---

## 2026-08-14 · hanliyagi — YP2021 프로토콜 상세 v1.3 정본 반영

**무엇이 바뀌었나**
- 압축 실행 기준으로 되어 있던 v1.3 프로토콜을 사용자가 제공한 상세 최종본으로 교체했습니다. 코드·인터페이스·설정과 검증된 표본 수는 변경하지 않았고, 표본 수·직군 분포는 `⬜ 사람 최종 확인 필요`로 유지했습니다.

**팀원이 할 일**
- 전처리·모델링 구현 전 [`상세 v1.3 프로토콜`](plan/details/11-YP2021-공통-전처리-모델링-프로토콜.md)의 공통 기준과 §29의 후속 구현 범위를 확인해 주세요.

**자세히**: [`상세 프로토콜`](plan/details/11-YP2021-공통-전처리-모델링-프로토콜.md) · [`인터페이스`](plan/details/06-인터페이스.md)

---

## 2026-08-14 · hanliyagi — YP2021 프로토콜 v1.3 분석대상 변경

**무엇이 바뀌었나**
- 분석대상을 취업준비 중인 미취업자에서 기준연도 미취업자 전체로 바꿨습니다. 구직·취업준비 관련 변수는 표본 제한이 아닌 Feature로 유지하며, Person-Period·Global/Local Dataset·SAMPID split을 v1.3 기준으로 다시 생성했습니다.

**팀원이 할 일**
- 기존 3,278행 기준 Dataset·split·모델 결과는 사용하지 마세요. 개인 단위 파생 파일은 public 저장소에 올리지 않으므로, `data/result/README.md`의 명령으로 각자 v1.3 Dataset·split을 로컬에서 다시 생성한 뒤 Feature 구현 및 모델링을 이어가 주세요.

**자세히**: [`v1.3 프로토콜`](plan/details/11-YP2021-공통-전처리-모델링-프로토콜.md) · [`인터페이스`](plan/details/06-인터페이스.md) · [`결과물 안내`](data/result/README.md)

---

## 2026-08-14 · hanliyagi — 검토 완료 범위 표시

**무엇이 바뀌었나**
- 확인된 공통 규칙·KECO 매핑·Feature/모델 설정·데이터셋 인터페이스·분할/비교 기준만 문서의 `✅ 검토 완료`로 표시했습니다. 미구현 Feature와 고급 평가 구현은 미검토로 유지했습니다.

**팀원이 할 일**
- 미검토 또는 미구현으로 남은 항목을 완료했다면, 코드북 대조·테스트·팀 검토 후에만 해당 표시를 갱신해 주세요.

**자세히**: [`공용 인터페이스`](plan/details/06-인터페이스.md) · [`공통 프로토콜`](plan/details/11-YP2021-공통-전처리-모델링-프로토콜.md) · [`설정 안내`](code/config/README.md)

---

## 2026-08-14 · hanliyagi — source adapter 리뷰 반영

**무엇이 바뀌었나**
- YP2021 특수결측을 숫자 범위가 아니라 코드북 확인을 마친 변수별 규칙으로 처리하도록 바꿨습니다. `recent_employment_prep`·`recent_job_search`는 원자료가 미관측이면 0 대신 NA와 사유를 보존합니다.

**팀원이 할 일**
- 새 원변수를 연결할 때는 `code/config/yp2021_missing_rules.json`에 해당 변수의 코드북 특수코드를 먼저 추가해 주세요.

**자세히**: [`특수결측 규칙`](code/config/yp2021_missing_rules.json) · [`공용 인터페이스`](plan/details/06-인터페이스.md)

---

## 2026-08-14 · hanliyagi — YP2021 공통 파이프라인·인터페이스 뼈대 추가

**무엇이 바뀌었나**
- v1.2 프로토콜을 저장소의 공통 규칙·설정·인터페이스로 반영하고, 원자료 → Person-Period → Global/Local Dataset → SAMPID split까지 연결되는 실행 뼈대를 추가했습니다. 원자료 대조 결과 세 전환 표본 수(1,444 / 884 / 950행)도 프로토콜과 일치합니다.

**팀원이 할 일**
- 담당자는 `code/AGENTS.md`와 `plan/details/06-인터페이스.md`를 먼저 읽고 자기 모듈만 구현해 PR로 올려 주세요. 전공·이력형 Feature와 특수결측 매핑은 코드북 대조 전까지 임의로 0 처리하지 마세요.

**자세히**: [`공통 프로토콜`](plan/details/11-YP2021-공통-전처리-모델링-프로토콜.md) · [`인터페이스`](plan/details/06-인터페이스.md) · [`코드 사용법`](code/README.md)

---

## 2026-08-13 · hanliyagi — 희망직군 6개 전처리·모델링 계획 반영

**무엇이 바뀌었나**
- 8월 13일 회의록과 현재 계획을 갱신했습니다. 4차 응답자 9,665명 슬라이스, Local 데이터 약 3,921명 전처리, Local Model 6개·Global Model 1개의 담당자와 8/14~8/19 일정을 확인할 수 있습니다.

**팀원이 할 일**
- 담당자는 회의록의 기한을 확인해 주세요. 8/16 13:00 설문 문항 제작 회의에서 KECO 6개 매핑, 공통 평가 규칙, 설문 문항·표본 모집 방식을 확정해 주세요.

**자세히**: [`회의록`](회의기록/20260813-전처리-모델링-역할분담.md) · [`전처리·모델링 계획`](plan/details/10-YP2021-직군별-취업예측-계획.md)

---

## 2026-08-13 · hanliyagi — pre-push 자동 점검 복구

**무엇이 바뀌었나**
- `pre-push` 훅의 실행 권한을 복구합니다. 이제 푸시할 때 `업데이트.md` 요약 누락을 다시 자동으로 확인합니다.

**팀원이 할 일**
- 없음. main을 받으면 적용됩니다.

**자세히**: [`scripts/githooks/pre-push`](scripts/githooks/pre-push)

---

## 2026-08-13 · hanliyagi — YP2021 직군별 취업예측 계획 공유

**무엇이 바뀌었나**
- 팀원이 함께 볼 수 있도록 YP2021 공식 자료 링크, Global/직군별 Local 모델 비교 구조, 전처리·모델링 검토 항목을 계획 문서로 추가했습니다.

**팀원이 할 일**
- 문서의 미검토 항목을 읽고, KECO 직군 매핑과 공통 전처리·평가 기준을 회의에서 확정해 주세요.

**자세히**: [`YP2021 직군별 취업예측 계획`](plan/details/10-YP2021-직군별-취업예측-계획.md)

---

## 2026-08-12 · hanliyagi — 프로젝트 일정·직무별 모델링 계획 확정

**무엇이 바뀌었나**
- 회의록을 표준 형식으로 정리하고, 표본 30명 목표·8/12~8/22 일정·직무별 독립 전처리·모델링·데모 목표를 현재 계획에 반영했습니다.

**팀원이 할 일**
- 직무별 분류 기준과 담당자, 표본집단 선정·설문 담당자를 정해 주세요.

**자세히**: [`회의기록`](회의기록/20260812-프로젝트-진행계획-논의.md) · [`현재 계획`](plan/details/01-overview.md)

---

## 2026-08-10 · hanliyagi — 기획서 1차 수정본 반영

**무엇이 바뀌었나**
- 제출용 기획서를 1차 수정본으로 갱신했습니다. 프로젝트 목적과 분석 질문, `w04ecoact`의 취업/미취업 이진 변환, 평가 지표 설명이 더 구체적으로 정리됐습니다.

**팀원이 할 일**
- 제출 전 [`기획서`](plan/details/08-기획서-20260811.md)의 Recall 설명에 적힌 **“204년”**이 “2024년”의 오타인지 원본 작성자가 확인해 주세요.

**자세히**: [`plan/details/08-기획서-20260811.md`](plan/details/08-기획서-20260811.md)

---

## 2026-08-09 · NaHoYeol — 기록 자동화 3종 (커밋·푸시·풀)

**무엇이 바뀌었나**
- **깜빡해도 팀 규칙이 지켜지도록 git 훅 3개를 걸었다.** 이제 의식적으로 외울 필요가 없다.
  - `git commit` — 작업기록이 없으면 **막힌다**
  - `git push` — [`업데이트.md`](업데이트.md) 요약이 없으면 **막힌다** (기록 파일만 바뀐 푸시는 예외)
  - `git pull` — 새로 올라온 업데이트 요약이 **터미널에 자동으로 뜬다**
- 막힐 때마다 "AI에게 이렇게 말하면 됩니다" 안내가 함께 뜬다. 명령어를 외울 필요 없다.

**팀원이 할 일**
- **한 번만 해주세요**: 레포 폴더에서 `git config core.hooksPath scripts/githooks`
  (이미 했으면 그대로. AI를 쓰면 세션 시작할 때 알아서 설정됩니다)
- 앞으로 작업하고 푸시할 때 AI가 요약을 대신 써 줍니다. **다만 "팀원이 할 일" 항목은 판단이 들어가니 직접 읽고 고쳐주세요** (대원칙).

**자세히**: [`AGENTS.md`](AGENTS.md) §13

---

## 2026-08-09 · NaHoYeol — 연구 근거·설문 척도·팀 대원칙

**무엇이 바뀌었나**
- **주제의 '왜'가 문서로 정리됐다.** 청년이 취업 때문에 힘든 이유가 "자기 위치를 모르는 것"이라는 논리와, 그걸 뒷받침하는 취업 도메인 선행연구 5편을 붙였다 → [`plan/details/09-나호열-프로젝트계획.md`](plan/details/09-나호열-프로젝트계획.md) (팀원용 설명서, 쉬운 말로 씀)
- **실험에 쓸 설문 척도가 정해졌다.** 김고운 외(2023) 취업 스트레스 척도 **22문항 중 19문항** 사용 제안 (면접 3문항 제외). 문항 전문은 09 부록 B.
- **논문 4편을 레포에 넣었다.** 원문 PDF + md 변환본 → [`논문/`](논문/) 아래 4개 폴더.
- **팀 기획서가 레포에 들어왔다** → [`plan/details/08-기획서-20260811.md`](plan/details/08-기획서-20260811.md) (청년패널2021로 차년도 취업 여부 예측)
- ⭐ **팀 대원칙이 생겼다** → [`AGENTS.md`](AGENTS.md) 맨 위. **코드는 AI에게 맡기되, 논문 인용 이유·문항 선택·대상자 설계·방법론·결과 해석은 사람이 직접 쓴다.** 문서마다 「🖊 작성 출처」 표가 붙어 어디까지 AI가 썼는지 남는다.

**팀원이 할 일**
1. `09` 문서를 읽어 주세요. 우리가 왜 이걸 하는지가 거기 다 있습니다.
2. **피험자 섭외** — 1인당 지인 1~2명. 이미 취업한 사람은 제외.
3. 문서에 `⬜ 미검토` 표시가 있는 곳은 **AI가 쓴 초안**입니다. 자기 담당 부분은 직접 읽고 다시 써 주세요 (대원칙).
4. 정할 것 3건이 [`회의기록/안건.md`](회의기록/안건.md)에 있습니다 — 설문 대상 범위, 실험 채택 여부, 실험 설계 쟁점.

**자세히**: [`plan/CHANGELOG.md`](plan/CHANGELOG.md) 2026-08-09 항목 4묶음
