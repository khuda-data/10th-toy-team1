# 2026-08-18 오늘 작업 요약 — major_group 백필 버그 수정 / 오버샘플링 완화 / Local Model 스모크 테스트

> 이 문서는 오늘 세션 전체를 `main`에서 바로 요약해 둔 기록(문서 커밋, 브랜치 불필요)이다. **실제 코드 변경은 아직 `main`에 없고, 아래 3개 브랜치에 PR 리뷰 대기 중이다.** 각 작업의 상세 세션 로그는 해당 브랜치의 `작업기록/wjdwlsah/`에 있다(머지되면 `main`에도 따라온다).

- 도구/모델: Claude Code / Sonnet 5
- 브랜치: main (문서 전용 커밋)

## 지침 (내가 시킨 것)
1. `major_group` Group A 백필이 `NotApplicable` 재분류와 충돌해 983명 과거 전공값이 유실되는 버그(jungjinmo가 8/17 발견) 원인 분석·수정 요청.
2. Global 오버샘플링(41.3%가 59.3% 행 차지) 완화 방안 논의 → `n_prior_periods` feature + `sample_weight` 가중치를 같은 데이터셋에 함께 적용하기로 결정, 구현 요청.
3. Global 데이터셋 PR이 머지됐다고 가정하고 Local Model 데이터셋 전처리 지침을 팔로업, raw 데이터가 없어 가짜 데이터로 파이프라인 스모크 테스트 요청.
4. 세션 도중 로컬에 남아있던 미완료 `git revert`(사용자 실수) 발견 → 원상복구.
5. 오늘 작업기록·업데이트 로그를 `main`의 `작업기록/wjdwlsah`에 요약해서 커밋·푸쉬 요청.

## AI가 한 일 — 브랜치별 요약

### ① `feat/wjdwlsah-major-group-backfill` (PR 대기)
- `code/pipeline/build_person_period.py`의 `_backfill_group_a` 수정: baseline이 `NotApplicable`이어도 과거 유효값을 계속 찾도록 조건 확장. "가장 최근에 확인된 실제 전공을 쓴다"는 정의로 확정.
- PR: https://github.com/khuda-data/10th-toy-team1/pull/new/feat/wjdwlsah-major-group-backfill

### ② `feat/wjdwlsah-oversampling-mitigation` (PR 대기, ①에 의존)
- `person_period.parquet`에 `n_prior_periods`(Global 전체 이력 기준 1회 계산) 추가.
- `DatasetBundle`에 `sample_weight`(Global·Local·직군별 부분집합마다 각각 재계산) 필드 추가, `tune_model`/`train_model`에 `sample_weight_train` 인자 연결(학습에만 반영, 검증 fold 채점엔 미반영 — 팀 결정 필요 사안으로 안건함 등록).
- PR: https://github.com/khuda-data/10th-toy-team1/pull/new/feat/wjdwlsah-oversampling-mitigation

### ③ `feat/wjdwlsah-local-model-dataset` (PR 대기, ①②에 의존 — 〔머지가정〕)
- ①②를 로컬에서만 병합한 통합 브랜치에서, Local Model 전처리 지침(`11-...프로토콜.md` §12~17)과 코드(`build_local.py` 등)를 전수 대조해 전부 일치 확인.
- raw 데이터 없이 가짜 데이터로 전체 파이프라인 스모크 테스트 실행 — `n_prior_periods`·`sample_weight` 배관이 실제로 정상 동작함을 확인(특히 사람의 희망직군이 연도마다 바뀌는 엣지케이스에서 Local 전체 기준과 직군별 기준 가중치가 다르게 재계산되는 것 검증).
- PR: https://github.com/khuda-data/10th-toy-team1/pull/new/feat/wjdwlsah-local-model-dataset

### 그 외
- 세션 중간에 로컬 클론에 남아있던 미완료 `git revert`(`62eb88f` 되돌리기 시도, 다른 팀원 파일 삭제 위기)를 발견해 `git revert --abort`로 원상복구.

## 검증 (내가 직접 확인한 것)
- ①②의 모든 Python 변경은 `ast.parse` 문법 검증 통과.
- ③은 가짜 데이터로 실제 실행까지 확인(assert 전부 통과) — 다만 **실제 raw 데이터 재검증은 세 브랜치 전부 아직 안 됨**(이 컴퓨터에 YP2021 원자료 없음, Downloads·Documents 등 재검색해도 없음).

## 남은 것 / 막힌 것
- 3개 PR 전부 팀원 리뷰 대기 중 — 리뷰·머지는 사람이 직접(셀프 머지 금지).
- 머지 순서: ① → ② → ③ (③은 ①②에 의존).
- raw 데이터 있는 팀원이 실제로 파이프라인을 재실행해서 `11-...프로토콜.md` §14 Local 표본 수(8/14 스냅샷) 갱신, `major_group` 983명 중 실제 회복 인원 확인 필요.
