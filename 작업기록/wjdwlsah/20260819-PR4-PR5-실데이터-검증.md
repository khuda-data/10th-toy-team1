# 2026-08-19 PR #4·PR #5 실데이터 검증 + PR #5 반영 확인

> 이 문서는 `main`에서 바로 요약해 둔 기록(문서 커밋, 브랜치 불필요). 아래 검증은 전부
> 로컬 임시 worktree/스크립트로 진행한 읽기 전용 확인이라 이 브랜치(`main`)나
> `feat/wjdwlsah-*` 코드 자체에는 변경이 없다.

- 도구/모델: Claude Code / Sonnet 5
- 브랜치: main (문서 전용 커밋)

## 지침 (내가 시킨 것)
1. PR #4(`major_group` 백필 수정)에서 ① 983명 중 과거 실제 전공값 있는 사람이 정상 복구되는지 ② Person-Period 총 행 수 14,906이 변하지 않는지 ③ 다른 Feature 값이 의도치 않게 바뀌지 않는지 세 가지 확인 요청.
2. `C:\Users\USER\Documents\KHUDA_10기\Toy_project\데이터셋`에 실제 YP2021 raw 데이터가 있음을 알려주고, 이 데이터로 다시 확인해 달라고 요청.
3. PR #5(오버샘플링 완화)에서 `n_prior_periods`·`sample_weight`가 항상 같이 적용되는 구조를 지적하고, 세 실험(①Baseline ②+n_prior_periods ③+sample_weight)을 독립적으로 비교할 수 있게 분리해 달라고 지시(상세 지침·구현·검증은 `작업기록/wjdwlsah/20260818-오버샘플링-완화-n_prior_periods-sample_weight-구현.md` 참조).
4. PR #5가 요청한 내용대로 실제로 바뀌었는지 GitHub에서 직접 확인해 달라고 요청.

## AI가 한 일
- **PR #4 검증**: raw 데이터(`YP2021_w01~w04.xlsx`)를 표준화해 캐싱한 뒤, main과 PR #4(`feat/wjdwlsah-major-group-backfill`) 두 버전의 `build_person_period.py`를 그대로 실행해 결과를 비교.
  - `major_group=NotApplicable` 4,716행 중 **983행이 복구**(2022 baseline 345 · 2023 baseline 638) — jungjinmo의 8/17 원 보고와 정확히 일치.
  - Person-Period 총 행 수 old/new 모두 **14,906행**으로 동일, `baseline_year`별 구성(2021=6,010/2022=4,916/2023=3,980)까지 일치.
  - 전체 컬럼 diff 결과 **변경된 컬럼은 `major_group` 하나뿐**(정확히 983행) — 다른 Feature는 전혀 안 바뀜.
- **PR #5(원안) 실데이터 검증**: 같은 raw 데이터로 `n_prior_periods`·`sample_weight` 계산 로직을 직접 실행해 값이 정상 산출되는지 확인하던 중, 사용자가 두 장치의 분리 필요성을 제기해 구현 방향이 바뀜 — 이후 작업은 `20260818-오버샘플링-완화-n_prior_periods-sample_weight-구현.md`에 이미 기록·커밋·푸시됨(`feat/wjdwlsah-oversampling-mitigation`에 커밋 2개 추가).
- **PR #5 반영 확인**: `gh` CLI가 없어 GitHub 웹을 직접 조회 — PR #5(https://github.com/khuda-data/10th-toy-team1/pull/5) 커밋 목록에 오늘 추가한 커밋 2개(`n_prior_periods·sample_weight를 독립적으로 켜고 끌 수 있게 분리`, `raw 데이터로 n_prior_periods/sample_weight 분리 검증 결과 기록`)가 실제로 올라가 있음을 확인. 요청 항목(자동 동시 적용 방지·선택적 Feature 포함·옵션 기반 가중치·기본 42개 재현·모델별 이름 분리) 전부 반영됐음을 코드·검증 결과 대조로 확인.

## 검증 (내가 직접 확인한 것)
- PR #4: old/new `build_person_period.py`를 실제로 import·실행해 비교(재구현 없음). 983/345/638, 14,906행, `major_group` 외 무변경 — 세 가지 전부 실측치로 확인.
- PR #5: GitHub 커밋 목록을 직접 조회해 로컬에서 만든 커밋이 실제로 PR #5에 반영됐음을 확인(추측 아님).
- 검증에 쓴 스크립트·캐싱 데이터는 전부 `%TEMP%\claude\...\pr4_verify\`(레포 밖, 커밋 대상 아님)에 있고, 사용한 임시 worktree·브랜치는 검증 후 전부 삭제함.

## 남은 것 / 막힌 것
- PR #4·PR #5 모두 아직 팀원 리뷰·머지 전 — 리뷰 요청은 `업데이트.md`에 이미 안내돼 있음.
- `tune_model`의 `sample_weight_train`이 `GridSearchCV` 검증 fold 채점(scoring)에는 반영되지 않는 점은 여전히 팀 미확정 사안으로 안건함에 남아 있음.
