# 2026-08-23 — Local Group3(교육·법률·사회·공공) 모델링 환경 문제 해결 + PR 준비

## ① 내가 준 지침 (요지)

- matplotlib이 VSCode 노트북에서 `ModuleNotFoundError` 남 → 원인 파악·해결 요청
- 이어서 `code.model.first_stage` 모듈을 못 찾는 에러 → 원인 파악·해결 요청
- `local_dataset.parquet`을 못 찾는 `FileNotFoundError` → 해결 요청
- `sandbox/choi-1110/03_second_model_local.ipynb`에서 다른 팀원(didwo)의 OneDrive 절대경로가 하드코딩된 셀 실행 시 에러 → 원인 설명과 수정 요청, 이어서 `stage_2_paths['summary']`가 정확히 어떤 경로인지 질문
- `03_5_final_tuning.ipynb`(Stage 3.5)에서 `stage_3_local_group1` 관련 파일이 없다는 에러 → "지금 어느 직군을 진행하려는 거야?" 질문에 **"후자"(이미 끝낸 교육·법률·사회·공공/group3의 Stage 3.5 진행)**로 확정
- 같은 에러가 반복될 때 "cell1 다시 실행했어"라고 알려줌 (커널 변수 갱신 여부 확인 목적)
- LR·XGBoost "튜닝"이 정확히 무슨 뜻인지 질문
- **최종 요청**: `sandbox/choi-1110`의 노트북 3개(`03_second_model_local`, `03_5_final_tuning`, `04_model_comparison`)로 그룹3 모델링을 끝냈으니, 팀원들이 결과·코드를 볼 수 있게 PR을 만들어 머지 요청하고 싶다 — "어떤 파일을 하면 돼?"
  - PR 포함 범위 확인 질문에 **"그�룹3 관련만"**(8/16자 leftover·원자료 zip 제외) 선택
  - 브랜치 확인 질문에 **"새 브랜치 `feat/choi-1110-local-group3-modeling`"** 선택 (기존 `feat/choi-1110-cert-training-workexp-features`와 주제가 달라 분리)

- (추가) "1시간 전에 cepil이 group1에 대해서 pr만든거 이것처럼 하고 싶다"는 요청 → `origin/feat/Cepil-local-tuning` 브랜치의 실제 커밋·diff를 직접 조회해 구조(직군별 `sandbox/<아이디>/<영문라벨>/` 하위폴더 + 폴더별 README + `stage_3/3.5/4_local_group*` 결과 폴더 + 작업기록)를 확인하고 그대로 맞춤

## ② AI가 한 일

1. **matplotlib 미설치 진단**: 이 PC에 Python이 3개(3.11, py 기본 3.14, uv 관리 3.14.3) 깔려 있고 `pip install`이 3.11에만 들어갔던 것을 확인 → `py -3.14`(uv 3.14.3과 동일 버전)에도 `code/requirements.txt`를 추가 설치.
2. **`code.model.first_stage` ModuleNotFoundError 진단**: 이 PC에 저장소 복사본이 **두 개**(`C:\Users\pc\10th-toy-team1`, `C:\Users\pc\Desktop\10th-toy-team1`) 있고, VSCode가 열고 있던 **Desktop 쪽이 main보다 35커밋 뒤처진 오래된 브랜치**라 `code/model/first_stage.py` 자체가 없었던 것을 확인 → 사용자 선택에 따라 Desktop 저장소에서 `origin/main`을 현재 브랜치(`feat/choi-1110-cert-training-workexp-features`)로 fast-forward merge(충돌 없음, 35커밋).
3. **`local_dataset.parquet` FileNotFoundError 진단**: `data/result/baseline_42features/datasets/`가 통째로 없는 것을 확인(예전에 만들어졌던 `data/result/datasets/*.parquet.parquet`은 구버전 파이프라인이 남긴 흔적으로, 지금 구조와 경로·이름이 다름) → `code/README.md`가 안내하는 대로 `python -m code.pipeline.run_pipeline --raw-zip "YP2021_EXCEL_0227.zip"`을 백그라운드로 실행해 `global_dataset.parquet`·`local_dataset.parquet`·`person_period.parquet`·`split_ids.csv`를 올바른 경로에 생성.
4. **didwo 하드코딩 경로 진단**: `03_second_model_local.ipynb` cell 6의 `csv_file_path`가 다른 팀원의 OneDrive 절대경로("본인 경로로 바꿔주세요" 안내 주석이 있는 템플릿 예시값)로 남아있던 것을 확인 → `csv_file_path = stage_2_paths['summary']`로 고쳐서, 어느 PC·어느 그룹으로 돌리든 cell 5가 실제로 저장한 경로를 자동으로 따라가게 함.
5. **Stage 3.5 group1/group3 불일치 진단**: `03_5_final_tuning.ipynb` cell 1·2가 `stage_3_local_group1`(경영·사무·금융) 기준으로 짜여 있었지만, 실제로 완료된 Stage 3 결과는 `stage_3_local_group3`(교육·법률·사회·공공)뿐이었던 것을 디스크 확인으로 발견 → 사용자가 "후자"(group3)를 선택해, `STAGE_3_DIR`·`OUTPUT_DIR`·필수파일 목록 2곳·`target_group`을 전부 `group3`/`'교육·법률·사회·공공'`으로 일치시킴.
6. 이후 같은 에러가 반복 보고됐을 때, 디스크의 파일 내용과 사용자가 보여준 트레이스백을 대조해 **"파일은 이미 고쳐져 있지만 cell 1을 다시 실행하지 않아 커널 메모리의 옛 변수 값이 남아있는 것"**임을 짚어줌(수정 아님, 진단).
7. **PR 준비**: `git status`로 현재 untracked 파일 전체(노트북 3개, `data/result/.../stage_3_local_group3`·`stage_3_5_local_group3`·`stage_4_local_group3`, 8/16자 leftover 3종, 원자료 zip)를 확인하고, 8/16자 leftover와 zip을 제외한 그룹3 관련 산출물만 포함하기로 사용자와 합의 → `feat/choi-1110-local-group3-modeling` 브랜치를 현재 HEAD(=main과 동일 커밋)에서 새로 생성.
8. **Cepil PR 구조 맞추기**: `git fetch`로 `origin/feat/Cepil-local-tuning`의 실제 커밋·diff를 조회 — Cepil은 `sandbox/cepil/economy/`처럼 **직군별 하위폴더 + 그 폴더 안 README.md** 구조로 PR을 냈다(단, README 내용은 `notebooks/global/README.md`를 그대로 복사해 제목이 "economy"가 아니라 "global"로 남아있는 실수가 있었음 — 그대로 따라 하지 않고 실제 내용에 맞게 새로 씀). 이를 참고해 `sandbox/choi-1110/`의 노트북 3개를 `sandbox/choi-1110/local_group3/`로 옮기고, 그 폴더 실제 내용에 맞는 README.md를 새로 작성함.

## ③ 검증 방법과 결과

- 각 진단은 실제로 파일 시스템을 직접 확인(`find`, `git log`, `git status`, notebook JSON 파싱)해서 내렸다 — 추측으로 답하지 않음.
- Desktop 저장소 merge는 `git merge origin/main --no-edit` 실행 후 `git log`·`git status`로 fast-forward·충돌 없음을 확인.
- 파이프라인 실행 후 `data/result/baseline_42features/{datasets,splits}/` 아래 4개 파일이 실제로 생성됐는지 `find`로 확인.
- 노트북 수정은 매번 JSON을 다시 읽어 수정된 줄이 정확히 반영됐는지, `json.load`로 파일이 깨지지 않았는지 확인.
- Stage 3 결과 존재 여부는 `find data/result/.../modeling`으로 `stage_3_local_group1`은 없고 `stage_3_local_group3`만 있음을 직접 확인.
- PR 포함 파일 후보는 `git status --short` + 파일 mtime(`ls -la`)으로 8/16자 vs 8/22~23일자를 구분해 근거를 확보.

## ④ 남은 것

- **아직 commit·push·PR 생성 전 단계다.** 다음 커밋에서: 노트북 3개 + `data/result/baseline_42features/modeling/{stage_3_local_group3,stage_3_5_local_group3,stage_4_local_group3}/`만 스테이징 → 커밋 → push → 터미널에 뜨는 PR 생성 링크를 사용자에게 전달 예정.
- 8/16자 leftover(`sandbox/choi-1110/global_feature_profile_2026081*.csv`, `data/result/{feature_importance,metrics,models}/`)와 원자료 zip(`YP2021_EXCEL_0227.zip`)은 이번 PR에서 제외 — 필요하면 나중에 별도로 정리.
- `feat/choi-1110-cert-training-workexp-features` 브랜치(자격증·훈련·경력 feature 작업)는 이번 세션에서 건드리지 않았고, 그쪽 PR 관련 진행 상황은 별도로 확인이 필요.
- `업데이트.md` 항목 추가는 push 시점에 함께 처리해야 함(pre-push 훅이 요구).
