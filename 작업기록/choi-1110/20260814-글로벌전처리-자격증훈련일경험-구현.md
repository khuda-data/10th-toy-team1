# 2026-08-14 글로벌 모델 전처리 구현 — 자격증·직업훈련·일경험

- 도구/모델: Claude Code / Sonnet 5
- 브랜치: `feat/choi-1110-cert-training-workexp-features`

## 지침 (내가 시킨 것)

- (이전 세션 `20260814-글로벌모델-전처리-설계.md`에서 이어짐) `plan/details/11-YP2021-공통-전처리-모델링-프로토콜.md`를 참고해 Global Model 전처리 스크립트를 작성해달라고 요청.
- 27개 미구현 feature 중 "자격증·훈련·일경험부터 먼저 짜줘"로 범위 지정. "취업에 중요한 요소가 뭔지 나한테 알려줘, 판단은 내가할게"도 함께 요청(별도 답변 예정).
- "직업훈련·경험일자리 문항이 1차 조사엔 없으면 코드를 어떻게 짜는지" 질문 → 결측 처리 규칙(전부결측 시 에러로 차단 / 일부결측은 Train median·`Missing` 카테고리로 자동 처리)을 설명하고 진행.
- "이제 어디 접근할 때 내 허락 받지말고 바로 들어가도 돼" — 파일 접근 등에 대한 사전 승인.

## AI가 한 일

1. **코드북·매핑표 대조**로 실제 원자료 문항 확인 (`[YP2021]…코드북_0227.xlsx`, `…매핑표_0227.xlsx`):
   - `e301`(자격증 취득여부)·`e302`(개수)·`e307_1`(1번째 자격증 전공관련도) — **1차(2021) 문항 없음**, 2~4차만 있음.
   - `e101`(직업교육훈련 경험여부)·`e102`(횟수)·`e108_1`(1번째 훈련 총시간) — **1차 없음**, 2~4차만.
   - `d001`/`d002`(경험일자리 유무·횟수, 2~4차)와 `d501`/`d502`(같은 내용의 1차 전용 회고조사 문항) — 짝을 이루는 문항이라 wave별로 다른 컬럼을 씀.
   - `c720`(취업준비 스펙_업무·직무 자격증)은 **1~4차 전부**에 있어서, 자격증 계열 중 유일하게 1차도 커버 가능. `a207`(졸업자 버전, 2~4차)과 OR 결합.
2. `code/pipeline/source_adapter.py` 수정:
   - `_source_columns()`에 위 원자료 컬럼 추가.
   - `_conditional_zero()` 헬퍼 신설 — "여부"가 2(없다)면 하위 개수·시간을 논리적 0으로, 1(있다)이면 실제값, 여부 자체가 결측이면 NA로 채움 (protocol의 "비해당은 논리적 0인 경우만 0" 규칙 적용).
   - `standardize_annual_frames()`에 9개 feature 매핑 추가: `has_certificate`, `certificate_count`, `has_major_related_certificate`, `has_employment_certificate`, `has_vocational_training`, `vocational_training_count`, `vocational_training_hours`, `ever_worked_before`, `past_job_count`.
3. `code/config/yp2021_missing_rules.json`에 위 9개 feature용 특수결측 규칙(9090908/9090909→Missing, blank→NotApplicable) 변수별로 등록.
4. `python -m compileall -q code`로 문법 검사 통과 확인.
5. `python -m code.pipeline.run_pipeline --raw-zip <choi-1110 로컬 원자료 zip>`을 두 번 실행(수정 전/후)해 실제 데이터로 검증.

## 판단이 필요한 지점 〔AI 제안 · 사람 검토 필요〕

- **`has_major_related_certificate`의 "관련 있음" 기준**: `e307_1`(전공 관련도 1~4점, 1=전혀없음~4=매우깊음)에서 **3점 이상**을 "관련 있음(1)"으로 매핑함. 이 임계값(3점)은 AI가 합리적으로 골랐을 뿐 팀이 확정한 기준이 아님 — 다르게 볼 여지 있음(예: 4점만 인정).

## 검증 (내가 직접 확인한 것)

파이프라인 실행 결과(`data/result/datasets/global_dataset.parquet`, 3,278행)에서 baseline_year별 결측률 확인:

| feature | 2021(구간1) 결측률 | 2022(구간2) | 2023(구간3) |
|---|---|---|---|
| has_certificate / certificate_count / has_major_related_certificate | 100% (문항 없음, 예상대로) | 0% | 0% |
| has_vocational_training / count / hours | 100% (문항 없음, 예상대로) | 0% | 0% |
| **has_employment_certificate** | **4.6%만 결측** (c720 워크어라운드로 1차도 커버됨) | 0% | 0% |
| ever_worked_before / past_job_count | 74.8%(d501이 1차 응답자 중 일부에게만 물어봐서 부분 결측, 조작 안 함) | 0% | 0% |

- `has_certificate` 값분포(2022·2023): 0(없음) 1,617 / 1(있음) 217 — 코드가 정상적으로 값을 채우는 것 확인.
- 결측 처리 방식(전부결측 컬럼은 `build_preprocessor`가 에러로 차단, 일부결측은 Train median/`Missing` 카테고리로 자동 처리)이 사용자에게 설명한 대로 작동함을 확인.
- `data/result/`의 parquet·csv 산출물은 `data/result/README.md` 규칙("큰 parquet·모델 파일은 커밋하지 않는다")에 따라 **커밋하지 않음**. 재생성 명령: 위 5번 항목의 `run_pipeline` 명령 (raw-zip은 개인 로컬 경로라 팀 공용 아님 — 원본 데이터 출처는 아래 "남은 것" 참조).

## 남은 것 / 막힌 것

- **일경험 그룹 중 2개는 이번에 구현 안 함**:
  - `school_work_experience`(재학중 일경험 유형·`a186_1`/`a616_1`) — 이 문항이 "경험 없음"을 답할 수 있는 문항인지, 아니면 상위 게이팅 질문이 따로 있어 결측이 "없음"과 "질문 자체를 안 받음"을 구분 못 하는 건지 코드북만으로 확정 못 함. 추정으로 채우지 않고 남겨둠.
  - `past_work_months`(총 근무 개월) — 직접적인 "총 근무기간" 변수가 없고, 근무 시작/종료 시점(년·월)을 일자리별(최대 3개)로 조합해 계산해야 함. 여러 일자리 기간을 어떻게 합산할지(단순합·중복기간 처리 등) 설계 판단이 추가로 필요해 이번 배치에서 보류.
- 나머지 미구현 feature 21개(전공 대계열 `major_group`, `months_since_graduation`, `prep_effort_01~12`+`prep_effort_other`, `graduation_prep_experience`, `graduation_job_search_experience`, 시험준비 3종)는 아직 손 안 댐 — 다음 배치.
- PR 아직 안 올림 — 다음 단계에서 push 후 PR 링크 전달 예정. 업데이트.md 항목도 함께 추가 필요.
- "취업에 중요한 요소가 뭔지 알려달라"는 요청은 이번 로그에선 다루지 않음 — 별도 답변 예정(모델 학습 전이라 아직 실제 변수중요도가 없음, 선행연구·기술통계 기반으로 제안할 예정).
