# 2026-08-16 PR 생성 + 피처 엔지니어링 요약 문서 작성

- 도구/모델: Claude Code / Sonnet 5
- 브랜치: `feat/choi-1110-cert-training-workexp-features`

## 지침 (내가 시킨 것)

- "지난번 데이터 전처리 때 보완해야 할 부분 뭐라고 했었지?" — 지난 세션(8/15)에서 안건함에 등록된 미해결 사항 확인 요청.
- "회의기록/안건.md에 내가 올린게 없는데?" — 안건함 항목이 사람이 직접 쓴 게 아니라 AI가 자동으로 올린 것임을 확인.
- "그럼 그거 우선 커밋하고 회의기록/안건.md에 푸시해줘" → 이미 커밋·푸쉬돼 있음을 확인해 보고.
- "깃허브 들어가면 내가 쓴거 안보이잖아" → main에 안 보이는 이유(머지 안 된 feature 브랜치에만 있음)를 설명.
- "PR열어주고 main에 푸시해줘" → PR은 열되, main 직접 머지는 AGENTS.md §8("리뷰 없는 셀프 머지 금지")에 따라 하지 않음을 안내.
- "피처 엔지니어링을 해야한다 얘기가 나왔는데 그거 우리가 코드를 짰잖아 그래서 팀원들한테 그거 보여주고 싶어" → 방식 확인(AskUserQuestion) 후 "코드+근거 요약 문서 작성" 선택.

## AI가 한 일

1. `gh pr create` 시도 → 이 환경에 `gh` CLI 미설치로 실패. 대신 GitHub compare URL(`.../compare/main...feat/choi-1110-cert-training-workexp-features`)을 안내해 사용자가 직접 "Create pull request" 누르도록 함(머지는 사람 몫).
2. `plan/details/12-Global-피처엔지니어링-요약.md` 신설 — 8/14~15 세션 로그(`작업기록/choi-1110/20260814-*.md`, `20260815-*.md`)와 실제 코드(`code/pipeline/source_adapter.py` 등)를 근거로 구현한 feature 31개, 핵심 규칙 2가지, 발견한 버그, 검증 결과를 정리. "확인 필요" 3건은 기존 안건함 내용 그대로 〔AI 제안 · 사람 검토 필요〕로 표시해 옮김(새로 지어내지 않음).
3. 루트 `README.md` 파일 지도에 위 문서 한 줄 등록 (AGENTS.md §10).

## 검증 (내가 직접 확인한 것)

- (사용자가 직접 검증할 항목 — 아직 검증 전)

## 남은 것 / 막힌 것

- `plan/details/12-Global-피처엔지니어링-요약.md`는 아직 팀원 검토 전(⬜). PR 리뷰 시 함께 확인 필요.
- PR은 링크만 안내한 상태 — 사용자가 직접 "Create pull request" 버튼을 눌러야 실제로 생성됨.
- `gh` CLI가 이 로컬 환경에 없다는 것 확인(다음에 필요하면 설치 여부 재확인).

## 이어서: main 직푸시 + 브랜치 충돌 발견 (2026-08-16)

### 지침
- "main 브랜치에 푸시해줘" → 위 요약 문서·README 등록을 main에 직접 커밋·푸쉬(문서라 AGENTS.md §2-5 허용). 그 사이 팀원(jungjinmo·hanliyagi)이 올린 커밋 9개와 README.md 한 줄이 겹쳐 충돌 → 두 변경 다 살리는 방향으로 직접 병합해 해결.
- "그럼 우리가 31개 피처 코드도 다 짠거면 지금 올린거로 팀원이 쓸 수 있는거야?" → 아니라고 답변(main엔 설명 문서만 있고 코드는 feat 브랜치에만 있음). 실제로 머지 가능한지 확인하려고 `git pull origin main`을 **feat 브랜치 위에서** 실행해버려 실제 병합 시도가 걸림 → 충돌 확인 후 `git merge --abort`로 즉시 되돌림(커밋 없음, 원상 복구 확인).

### AI가 한 일
- 병합 시도(되돌림)로 `code/pipeline/build_person_period.py`·`plan/details/06-인터페이스.md`·`plan/details/11-YP2021-공통-전처리-모델링-프로토콜.md`·`업데이트.md`·`회의기록/안건.md` 5개 파일 충돌 확인.
- 그중 `build_person_period.py`는 텍스트 충돌을 넘어 **로직 자체가 다름**: choi-1110 브랜치는 구직·취업준비 필터만 삭제, hanliyagi의 main(v1.3)은 같은 필터 삭제 + `target_ecoact.isin([1,2,3])` 조건을 새로 추가. AGENTS.md §8("충돌은 사람이 읽고 결정")에 따라 AI가 임의로 합치지 않고, `회의기록/안건.md`에 안건으로 등록만 함.
- `회의기록/안건.md`에 등록한 안건도 main에 직접 커밋·푸쉬(문서).

### 남은 것 / 막힌 것
- **PR 머지 전 반드시 choi-1110·hanliyagi가 `build_person_period.py`의 적격 대상자 로직을 직접 대조해서 하나로 합의해야 함.** AI가 대신 결정하지 않음.
- 문서 4개(`06-인터페이스.md` 등)의 충돌도 머지 시점에 사람이 확인 필요.
- 위 발견 내용을 `업데이트.md`에도 추가해 팀 전체에 알림 (AGENTS.md §13).

### 다음 세션에서 할 일 〔choi-1110이 직접 지시〕

choi-1110 원문: "결국 이따가 해야할 거는 팀원끼리 정한 새로운 규칙 반영, 과거 결측치에 대해서는 어떻게 처리할지 3개의 케이스로 새로 분류, 대체값으로 대체가 안되는 피쳐는 팀원이 진행한 상관관계 분석으로 결측치 채움 이 3가지 규칙이 올라오면 이걸 참고해서 피쳐 엔지니어링 다시 해보자"

팀원이 아래 새 규칙을 회의기록/안건.md 또는 팀 채팅에 올리면, 그걸 반영해 피처 엔지니어링(`code/pipeline/source_adapter.py` 등)을 다시 함:
1. 팀원끼리 새로 정한 규칙 반영 (내용 미정 — 올라오면 확인)
2. 과거 결측치 처리를 3개 케이스로 새로 분류 (기준 미정 — 올라오면 확인)
3. 대체값(Train median 등)으로 못 채우는 feature는 팀원이 진행한 상관관계 분석 결과로 결측치를 채움

이번 세션에서 발견한 `build_person_period.py` 로직 충돌(위 항목)도 이 새 규칙 반영 작업에서 같이 정리될 가능성 높음.
- 위 발견 내용을 `업데이트.md`에도 추가해 팀 전체에 알림 (AGENTS.md §13).
