# 2026-08-19 · NaHoYeol — YP2021 원자료 수령 + 공통 파이프라인 첫 실행

## ① 내가 준 지침 (프롬프트 요지)

1. "여태까지 깃에 업데이트 되어 있는 내용을 다 다운받아봐." → `git pull`
2. "여기서 데이터랑, 현재 학습한 모델까지 다운받아져 있니?" → 레포에 데이터·모델이 있는지 확인
3. "우리팀이 사용한 원본 데이터셋이 어디서 다운받는지 깃에 있니?" → 출처 문서 확인
4. **"`C:\...\26-2 10기\YP2021_EXCEL_0227.zip` 이거 처리해봐."** → 원자료를 직접 레포 루트에 놓고 처리 요청

## ② AI가 한 일

### git pull (커밋 55개 수령)
- `e0331a1` → `7ffd95a`, 71개 파일 +5,474줄, fast-forward (충돌 없음)
- `code/` 파이프라인 30개 파일이 처음으로 로컬에 들어옴
- 원격 feature 브랜치 6개도 함께 fetch

### 레포에 없는 것 확인
- `data/`는 README 3개뿐 — 원자료·파생 데이터셋·split 전부 미커밋 (public 저장소 정책, `.gitignore`)
- **학습된 모델 파일은 애초에 저장되지 않음** — `code/`에 `joblib.dump`·`pickle` 호출이 한 줄도 없다.
  `run_pipeline.py`가 남기는 건 `metrics.csv`·`feature_importance.csv`·`best_params.json`뿐이고
  모델 객체는 매 실행마다 새로 학습되고 사라진다.

### 원자료 보안 조치 (⚠️ 중요)
- 사용자가 36MB `YP2021_EXCEL_0227.zip`을 **레포 루트에 직접 배치**했는데,
  `.gitignore`가 `*.zip`을 막지 않아 `git add .` 한 번이면 public 저장소에 개인 단위 원자료가 올라갈 상태였다.
- `.gitignore`에 `*.zip` 규칙 추가 → `git check-ignore`로 차단 확인.

### 실행 환경 구성
- Python 3.14.3. 없던 패키지 5개 설치: `scikit-learn 1.9.0` · `pyarrow 25.0.1` · `xgboost 3.4.1` · `lightgbm 4.7.0` · `matplotlib 3.10.9`
- zip 내부: `YP2021_w01~w04.xlsx` (4개) — `source_adapter.py:111`이 기대하는 파일명과 정확히 일치

### 파이프라인 실행
- `python -m code.pipeline.run_pipeline --raw-zip "<레포 루트>/YP2021_EXCEL_0227.zip"` (모델링 제외, 데이터셋 생성 단계만) → 종료코드 0
- 산출: `data/result/datasets/{person_period,global_dataset,local_dataset}.parquet` + `data/result/splits/split_ids.csv` (전부 `.gitignore` 대상이라 미커밋)

## ③ 검증한 방법과 결과

생성된 산출물을 pandas로 직접 읽어 **팀 문서에 적힌 수치와 대조**했다. 전부 일치 — 이 컴퓨터에서도 팀과 같은 데이터가 재현된다.

| 항목 | 이번 실행 | 팀 문서 기준 | |
|---|---|---|---|
| Person-Period 행 / 고유 SAMPID | 14,906 / 7,143 | 14,906 / 7,143 | ✅ |
| Local 행 / 고유 SAMPID | 4,603 / 2,838 | 4,603 / 2,838 | ✅ |
| Local target (유지 / 전환) | 2,970 / 1,633 | 2,970 / 1,633 | ✅ |
| baseline_year 분포 | 2021: 6,010 · 2022: 4,916 · 2023: 3,980 | wjdwlsah 8/19 검증치와 동일 | ✅ |

- Global 45열 / Local 49열(직군 정보 추가), split은 7,143명을 train 5,737 / test 1,406으로 분할.
- **Global 클래스 불균형 확인**: 미취업 유지 11,010 vs 취업 전환 3,896 (약 2.8:1). Local은 2,970 vs 1,633 (약 1.8:1).
  → 이 숫자가 뜻하는 바(모델링에서 어떻게 다룰지)는 **사람이 판단할 영역**이라 해석을 확정하지 않았다.

## ④ 남은 것 · 막힌 것

- **모델 학습은 돌리지 않았다.** 사용자가 "모델 학습을 돌리는 게 아니라, 우리 팀이 어디까지 작업했나 알고 싶어"라고 방향을 바꿔서 데이터셋 생성까지만 하고 멈췄다.
- **`.gitignore`의 `*.zip` 추가는 사용자가 "수정하지마. 냅둬"라고 해서 커밋 보류 상태였다.** 이번 마무리에서 함께 커밋할지 확인 필요.
- 매핑표·코드북 xlsx 2개는 이 컴퓨터에 없다. zip에는 조사 데이터(w01~w04)만 있다. 결과 해석 단계에서 필요.
- 원자료 zip은 레포 루트 → `설문/` 작업과 무관하게 루트에 그대로 있다(gitignore 처리됨).

---
## 🖊 작성 출처

> `AGENTS.md` 대원칙에 따른 기록. **⬜ 항목은 사람 검토 전이므로 확정된 내용이 아니다.**

| 구간 | 내용을 정한 주체 | 사람 검토 |
|---|---|---|
| ① 지침 | **사람(NaHoYeol)이 직접 지시** — 프롬프트 원문 기록 | ✅ 2026-08-19 NaHoYeol |
| ② 한 일 (사실 기록) | AI가 실행하고 기록 | ⬜ 미검토 |
| `.gitignore` zip 차단 판단 | AI가 public 저장소 정책(`data/raw/README.md`)을 근거로 판단해 선조치 | ⬜ 사람 확인 필요 |
| ③ 실행 결과 수치 | AI가 파이프라인 실행으로 산출 | ⬜ 사람 확인 필요 |
