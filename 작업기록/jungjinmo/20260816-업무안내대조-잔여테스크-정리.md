# 2026-08-16 업무안내 vs 파이프라인 대조 및 잔여 테스크 정리

- 도구/모델: Claude Code / Sonnet 5
- 브랜치: main (문서 작업, 코드 변경 없음)

## 지침 (내가 시킨 것)

세션 안에서 순서대로 요청한 내용:

1. 중복값 관련 지침이 적혀있는 문서를 찾아서 제시.
2. 어제 파일에 올라온 247명의 중복이 제시한 지침 중 어느 내용에 속하는지 파악.
3. 로컬에 있는 `C:\Users\USER\Documents\KHUDA_10기\Toy_project\YP2021_Global_Local_데이터셋_전처리_업무안내.md`의 Local Model 전처리 지침을 현재 GitHub에 올라온 파이프라인 코드가 전부 포함하고 있는지 확인.
4. (지금) 현재 해야 할 테스크들을 정리해서 다시 커밋·푸쉬.

## AI가 한 일

- `plan/details/11-YP2021-공통-전처리-모델링-프로토콜.md`에서 "중복" 키워드 관련 규칙을 찾아 두 가지로 구분했다 — ① 176~178행 이력형 변수(자격증·직업훈련 등) 반복 보고 중복 제거 규칙, ② 27행·37행 `SAMPID × baseline_year` 분석단위·그룹 분할 규칙.
- `작업기록/choi-1110/20260814-글로벌모델-전처리-설계.md`와 `작업기록/jungjinmo/20260816-YP2021-Local중복-Feature매핑-점검.md`를 대조해 247명 중복이 person-period 행 구조(②)에서 비롯된 것이지 이력형 변수 중복 집계(①) 문제가 아님을 확인했다.
- `Toy_project/YP2021_Global_Local_데이터셋_전처리_업무안내.md` 전체를 읽고, 그 안의 Local 지침(희망직업 선택 규칙·KECO 매핑·Global 부분집합 관계·공통 Feature 목록)을 실제 코드(`code/pipeline/build_local.py`, `code/pipeline/source_adapter.py`, `code/config/features.yaml`, `code/config/yp2021_missing_rules.json`)와 한 줄씩 대조했다.

## 검증 (내가 직접 확인한 것)

**일치하는 부분**
- Local = Global 부분집합, baseline까지 희망직업만 사용, 최신 응답 우선(`response_year` 내림차순), 같은 연도면 `c773z`(현재 준비) 우선 — `build_local.py:34-38` 확인.
- `job_group`은 `features.yaml`의 `features:` 목록에는 없고 `metadata_columns`에만 있음 — Feature로 넣지 않는다는 지침(업무안내 184~186행)과 일치.

**불일치(미구현) 부분**
- `features.yaml`에 정의된 42개 Feature 중 `source_adapter.py`가 실제로 원자료에서 뽑아오는 건 약 10개(`gender, age, region_5, education_level, student_status, student_type, university_type, job_seeker, recent_job_search, recent_employment_prep`)뿐이다. 자격증(4)·직업훈련(3)·시험준비(3)·일경험(4)·전공(`major_group`)·취업준비 활동유형(`prep_effort_*` 등) 총 31개는 원자료 매핑 코드 자체가 없다.
- `yp2021_missing_rules.json`도 14개 규칙만 있고, 위 31개 Feature용 특수결측 규칙은 없다.
- 업무안내 130행 "같은 자격증·일자리 경험이 여러 차수에서 반복 조사된 경우 중복 확인" 규칙도 자격증·일자리 이력 자체가 코드에 없어 자연히 미구현 상태.

## 남은 것 / 해야 할 테스크

**A. Feature 매핑 (최우선, 작업량 가장 큼)**
1. 코드북(`청년패널2021 1-4차 조사 코드북_0227(1).xlsx`, `매핑표_0227(1).xlsx`) 대조로 미구현 31개 Feature의 원자료 문항 코드 확정 — 자격증 4개, 직업훈련 3개, 시험준비 3개, 일경험 4개, 전공(`major_group`), 취업준비 활동유형(`prep_effort_01~12/other`, `graduation_prep_experience`, `graduation_job_search_experience`), `months_since_graduation`.
2. `source_adapter.py`(`standardize_annual_frames` 등)에 위 컬럼 표준화 코드 추가, 이력 누적 로직은 `build_person_period.py`와 조율.
3. `yp2021_missing_rules.json`에 신규 변수별 특수결측 규칙 추가.
4. 이력형 변수(자격증·직업훈련·일자리) 반복 보고 중복 확인 로직 구현 — 위 2번 작업과 함께 진행 (`plan/details/11-...프로토콜.md` 178행 규칙 반영).

**B. 결측치 재확인**
5. `student_type`(46.0% 결측)·`university_type`(38.7%)·`recent_employment_prep`(34.6%) — 실제결측인지 문항 비해당인지 코드북 재대조 필요. → **해결됨 (2026-08-16)**: 코드북 재대조 완료, 상세 내용은 [`과대표집·결측치 코드북재대조 세션 로그`](20260816-과대표집-결측치-코드북재대조.md) 참고.

**C. 팀 결정 필요 (→ `회의기록/안건.md` 등록)**
6. `작업기록/choi-1110/20260814-...` 문서의 "247명" 표기 — v1.2(구직조건 포함) 기준 폐기값. 현재 v1.3 기준 정확치(Global 2021∩2023=3,185명·2개 이상 등장=4,816명 / Local 2021∩2023=304명·2개 이상 등장=1,504명)를 팀에 공유하고 필요 시 문서 정정.
7. 세 구간(2021/2022/2023 baseline) 모두에 등장하는 사람의 과대표집 문제 — 삭제하지 않는다는 원칙은 있으나(choi-1110 기록), 구체적 처리 방침(가중치·플래그 등)이 아직 없음. Local 기준 규모가 1,504명(고유 2,838명의 53%)으로 알려진 것보다 큼. → 규모·구조를 더 정밀하게 재분석함, 상세 내용은 [`과대표집·결측치 코드북재대조 세션 로그`](20260816-과대표집-결측치-코드북재대조.md) 참고. **처리 방침 자체는 여전히 팀 결정 대기.**

**D. A 완료 후 후속**
8. `features.yaml` 42개가 모두 채워진 뒤 `--run-modeling` strict 모드로 재실행해 파이프라인 검증(현재는 미구현 Feature 때문에 strict 모드가 막힘).

---

## 🖊 작성 출처

> `AGENTS.md` 대원칙에 따른 기록.

| 구간 | 내용을 정한 주체 | 사람 검토 |
|---|---|---|
| 지침(질문 목록) | **사람(jungjinmo)이 직접 제시** | ✅ 2026-08-16 jungjinmo |
| 중복 규칙 분류·업무안내 대조 결과 | AI가 문서·코드를 직접 grep/대조해 산출 — 재현 가능 | ⬜ 팀 최종 확인 필요 |
| 잔여 테스크 목록(A~D) | AI가 코드-지침 gap 분석으로 도출, 우선순위는 AI 제안 | ⬜ 팀 판단 대기 |

- 세션 로그: 이 파일 자체가 세션 로그임
