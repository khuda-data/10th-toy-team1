# 2026-08-20 Global 모델링 단계별 프로토콜 게시

- 도구/모델: Codex / GPT-5
- 브랜치: main (문서 작업)

## 지침 (내가 시킨 것)

- “전체 구조는 아래와 같아”라며 Global 모델의 0~4단계 흐름을 제공했다: 42개 Feature의 LR/XGBoost 1차 모델 → 사람이 결과 판단 → 중요 Feature·공선성 분석 → 사람이 Feature 선택 → 선택 Feature 2차 모델 → 사람이 후보 판단 → 네 후보의 Test 비교와 사람이 최종 Global 모델 판단.
- `.py`는 재사용 계산·학습·평가 기능, `.ipynb`는 실행·표·그래프·관찰, 사람은 해석·Feature 선택·최종 모델 판단을 맡도록 팀원이 같은 구조로 진행하게 하고 GitHub에 올려 달라고 요청했다.

## AI가 한 일

- `plan/details/14-Global-모델링-단계별-실행흐름.md`에 사람 판단을 경계로 둔 Global 모델링 실행 순서를 새로 정리했다.
- `README.md`, `plan/README.md`, `plan/CHANGELOG.md`, `업데이트.md`에 새 공용 절차와 위치를 연결했다.
- 기존 공통 프로토콜·직군별 계획·코드 README에도 현재 Global 단계에서는 LR/XGBoost만 사용한다는 연결을 추가해, 기존의 공통 모델 지원 목록과 현재 실행 순서를 구분했다.

## 검증 (내가 직접 확인한 것)

- 최신 `main`을 fast-forward pull로 반영했다.
- 기존 11번 공통 프로토콜의 42개 기본 Feature, SAMPID 기준 분할, held-out Test 원칙 및 Bootstrap 규칙과 새 흐름이 충돌하지 않도록 대조했다.
- Markdown 링크 경로와 `git diff --cached --check`를 확인했고, 공백 오류 없이 문서 10개만 커밋 대상으로 스테이징했다. 기존 개인 실험 결과 폴더 4개는 건드리지 않았다.

## 남은 것 / 막힌 것

- 이번 작업은 운영 절차 문서화다. CV 시각화, Train 내부 Feature Importance·VIF 계산, ROC/PR·Bootstrap 구현은 각 Stage에 들어갈 때 공용 `.py` 기능으로 구현하고 Notebook에서 검증해야 한다.
- Stage 2의 실제 Feature 선택과 Stage 3·4의 모델 판단은 사람이 결과를 본 뒤에만 진행한다.
