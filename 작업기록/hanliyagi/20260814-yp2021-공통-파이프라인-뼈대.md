# 2026-08-14 YP2021 공통 파이프라인 뼈대

- 도구/모델: Codex / GPT-5
- 브랜치: `feat/hanliyagi-common-pipeline`

## 지침 (내가 시킨 것)

- “YP2021 공통 전처리·모델링 프로토콜 v1.2”를 기준으로 `code/` 아래 공통 코드 구조와 설정 파일, Person-Period부터 결과 저장까지 연결되는 최소 실행 뼈대를 만든다.
- Person-Period, 미래 정보 금지, Global/Local 공통 Feature·전처리·모델·평가, SAMPID Group Split을 공통 규칙으로 반영한다.
- 분석 대상·Feature·Target·희망직군·분할처럼 결과 의미를 바꾸는 기준은 임의로 변경하지 않는다. 실제 원자료 변수 매핑처럼 문서에 없는 사항은 확정하지 않는다.

## AI가 한 일

- 프로토콜 원문·기존 인터페이스·회의 결정과의 관계를 확인했다.
- `code/pipeline/`, `preprocess/`, `model/`, `evaluation/`, `config/`에 공통 모듈·설정·실행점을 추가했다.
- `plan/details/06-인터페이스.md`를 Person-Period·Global/Local·결과 파일 계약으로 교체하고, `plan/details/11-YP2021-공통-전처리-모델링-프로토콜.md`에 v1.2 실행 기준을 기록했다.
- 사용자가 알려 준 `Dataset&manual`의 원자료 zip·매핑표·코드북을 확인했다. Excel 파일의 비표준 `synchVertical` 속성은 원본을 건드리지 않는 임시 복사본에서만 제거해 읽도록 adapter에 처리했다.

## 검증 (내가 직접 확인한 것)

- `git pull origin main --ff-only`: 이미 최신 상태임을 확인했다.
- 프로토콜 원문: `/Users/hanliyagi1/Downloads/YP2021_공통_전처리_모델링_프로토콜_v1.2.md` 전체를 확인했다.
- 원자료의 `w**ecoact`, `y**c602`, `y**c635`를 대조해 적격 행이 2021→22 1,444행(취업 579), 2022→23 884행(392), 2023→24 950행(419)으로 프로토콜 표와 일치함을 확인했다.
- `python -m compileall -q code`: Python 문법 검사를 통과했다.
- 작은 in-memory DataFrame으로 `build_person_period_dataset()`의 SAMPID·기준연도·Target 생성 순서를 확인했다.

## 남은 것 / 막힌 것

- 전공 대계열, 자격증·직업훈련·일경험 누적, 1차와 2~3차 취업노력 문항의 동등성, 변수별 특수결측·설문 분기는 코드북 대조와 팀 검토가 필요하다. 이 기준이 완성될 때까지 `--run-modeling`은 누락 Feature를 명시하고 중단한다.

## 리뷰 반영 (2026-08-14)

### 사람이 전달한 판단 원문

> 현재 `_clean_special_missing()`에서 9,000,000 이상을 공통적으로 결측 처리하고 있는데, 프로토콜에서는 특수결측을 변수별 코드북 기준으로 처리하기로 했습니다. 이후 전공·자격증 등 다른 코드 변수에도 이 규칙이 적용될 수 있으므로, 하나의 범용 기준으로 처리하지 않고 변수별 결측 규칙을 적용할 수 있는 구조로 수정 부탁드립니다.

> `recent_employment_prep`, `recent_job_search` 생성 과정에서 원자료가 결측/비해당인 경우에도 `.eq(1)` 처리로 0이 될 수 있는 것 같습니다. `0=실제로 해당 없음`과 `Missing/NotApplicable`이 구분되도록 처리 부탁드립니다.

### AI가 한 일

- 코드북에서 `9090908=응답거절`, `9090909=모름`, 희망직업의 `9999997=분류불가`, `9999998=응답거절`, `9999999=모름`을 확인했다.
- `code/config/yp2021_missing_rules.json`에 원변수별 특수코드와 빈값 사유를 선언하고, `source_adapter.py`가 이 설정만 읽도록 수정했다.
- 최근 취업준비·구직활동 이진값은 실제 관측된 1/2 응답만 1/0으로 바꾸고, 모두 미관측이면 NA와 사유 컬럼을 남기도록 수정했다.

### 검증

- `python -m compileall -q code`로 변경된 모듈을 포함한 Python 문법 검사를 통과했다.
- 모의 원자료에서 실제 `있음/없음`은 각각 1/0으로, 두 원문항이 비어 있는 경우는 NA와 `NotApplicable`으로, 응답거절·모름은 NA와 `Missing`으로 남는 것을 확인했다.
- 같은 모의 원자료에서 희망직업의 `9999997~9999999`가 KECO 0~9의 유효값으로 유입되지 않으며, Person-Period 적격 판정이 NA를 취업준비자로 오인하지 않는 것을 확인했다.

## 검토 완료 표시 (2026-08-14)

### 사람이 전달한 판단 원문

> code/AGENTS.md의 공통 개발 규칙, KECO 6개 직군 매핑, features.yaml의 공통 Feature 목록 및 전처리 기준, model_config.yaml의 공통 모델·평가·분할 설정, plan/details/06-인터페이스.md의 Person-Period, Global/Local Dataset, split 및 결과 파일 계약, plan/details/11-YP2021-공통-전처리-모델링-프로토콜.md에서 이미 확정한 분석 대상, Target, 분할, Global/Local 비교 기준은 ✅ 검토 완료로 변경해도 됩니다.

> 실제 구현이 남아 있는 전공 대계열 변환, 자격증·직업훈련·일경험 누적, 변수별 세부 설문분기/특수결측 규칙, XGBoost의 fold별 scale_pos_weight, bootstrap CI 등은 아직 검토 완료로 표시하지 말고 미구현 또는 미검토 상태로 유지해주세요.

### AI가 한 일

- 전달받은 문서 범위만 `✅ 2026-08-14 검토 완료`로 바꾸고, 구현이 남은 원변수 매핑·이력형 Feature·XGBoost fold 가중치·bootstrap CI는 `⬜ 미구현 또는 미검토`로 남겼다.
