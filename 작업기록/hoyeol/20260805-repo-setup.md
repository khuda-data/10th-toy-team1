# 2026-08-05 팀 협업 레포 세팅 (1조 정정 · PDF→md · AGENTS.md · 디자인 라우팅 · 협업 규칙)

- 도구/모델: Claude Code / Fable 5
- 브랜치: main (문서·세팅 작업)

## 지침 (내가 시킨 것)

1. 레포 환경이 어디까지 만들어져 있는지 확인 + 깃허브 계정·레포 기록 확인.
2. 문서·폴더의 "10조" 표기를 "1조"로 수정해서 푸쉬 (폴더명 `git mv` 포함).
3. **"앞으로 모든 pdf 내용은 md로 변환해서 업로드하도록 해줘"** → 기존 PDF 3종 전부 변환 + 앞으로는 md만 업로드로 확정.
4. UXC 폴더(개인 작업 공간, 수정 금지·참고 전용)의 터미널 팁을 기반으로, 팀원들이 터미널 AI(클로드·제미나이·GPT 각자 사용)를 구동할 때 항상 지키는 기본 원칙을 세팅해서 푸쉬.
5. `DESIGN-apple.md` 푸쉬 + 앱·PPT 등을 만들 때 무조건 이 디자인을 자동으로 따르게.
6. 팀원 코드 병합·작업 공간 관리 방법 + AI 지침·답변의 버전 관리 방법 세팅. (세션 로그 폴더명은 "작업기록"으로 확정)
7. 지침·파일이 늘어날 때 못 찾는 문제 대비 + **레포 전체 구조 깔끔하게 정리** ("중구난방", plan 이원화 지적).
8. 팀원 공유용 온보딩 안내문 + 모든 AI 생성물에 "자연스러운 말투·쉬운 용어·구체적 설명" 스타일 지시.
9. 작업기록을 팀원이 아니라 **AI가 자동으로** 남기게 (의식적으로 못 지킬 것 대비).
10. `code/`(통합 코드)와 `data/`(raw·result) 신설 + 개인 산출물은 각자 작업 공간으로 분리 (`sandbox/<영문이름>/`).
11. "각자 만든 기능을 어떻게 합치나" → 웹·분석 등 **모든 협업에 통합 절차 적용** + 브랜치 사용법 질문.
12. 폴더별 설명이 처음 보는 사람 기준으로 충분한지 점검 + **새 폴더에는 만든 의도를 무조건 기록**하도록.
13. 막힌 것·토의 주제·회의 내용을 남기는 `회의기록/` 폴더 신설.

## AI가 한 일

- `4d4ff7e` 문서 내 10조→1조 수정 (5개 파일)
- `0894cd4` 폴더명 rename: `10조 YB 토이프로젝트` → `1조 YB 토이프로젝트`
- `8b72a7b` PDF 3종 md 변환(pymupdf4llm, 그림은 `assets/`) + `.gitignore`에 `*.pdf`(논문 원본 예외) + 해설 지침 갱신
- `c1aa341` `AGENTS.md`(AI 작업 원칙 단일 소스) + `CLAUDE.md`/`GEMINI.md` 포인터 + `DESIGN-apple.md` + plan Phase 2 로그
- `ba70507` AGENTS.md §8 코드 협업(GitHub Flow·폴더 소유권·sandbox), §9 AI 세션 기록 + `작업기록/` 신설
- `70eb09f` **레포 평탄화**: `1조 YB 토이프로젝트/` 래퍼 제거(plan·논문→루트), `구현 계획/`→`plan/details/04-구현계획-20260729.md`, DESIGN 2종→`design/`, `_fig_extract`→`scripts/fig_extract/`, 커밋된 PDF 2개 추적 해제. 루트 README = 파일 지도, AGENTS.md 빠른 라우팅 표 + §10(지도 유지·지침 분산)
- `e8e1414` `온보딩.md`(15분 코스) + AGENTS.md §11 AI 산출물 스타일(자연스러운 말투·쉬운 용어·구체성)
- `7c7dd99` **작업기록 자동화**: §9 강화(AI가 자동 기록·커밋마다 스테이징) + `scripts/githooks/pre-commit` 훅(로그 없는 커밋 차단, 차단 동작 테스트 완료). `code/`·`data/raw·result`·`sandbox/<영문이름>/` 신설 — 개인 산출물은 sandbox, `data/result`는 통합 코드의 공용·최종 결과물만
- (이번 커밋) **협업 통합 절차 표준화**: `plan/details/05-협업-통합절차.md`(4단계+체크리스트+브랜치 입문 부록) + `06-인터페이스.md`(약속 장부 템플릿) 신설, AGENTS.md §8 "합치는 순서"와 AI 규칙(약속 우선·mock 선개발) 추가, code/data README 연결

## 검증 (내가 직접 확인한 것)

- `git check-ignore -v`로 `*.pdf` 차단·논문 원본 예외 패턴 동작 확인
- 변환된 md 3종의 제목·표·코드블록·이미지 링크(ASCII 상대경로) 스팟체크
- 각 커밋 후 `git push` 성공 및 `git log` 확인

## 남은 것 / 막힌 것

- 해설 PDF 내부 푸터에 "10조" 잔존 (로컬 생성용이라 급하지 않음, 재생성 시 정리)
- 코드 작업 시작 시: `src/` 모듈 분담(폴더 주인) 팀 회의로 확정, 가능하면 khuda-data 관리자에게 main branch protection(PR 필수) 설정 요청
- 팀원 온보딩: clone → 각자 AI에서 "이 레포의 작업 원칙 요약해봐"로 AGENTS.md 로드 확인
